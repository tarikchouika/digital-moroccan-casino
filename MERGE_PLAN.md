# 📋 خطة الدمج — Digital Moroccan Casino
### مقارنة بين خطّي التطوير المتوازيين (محرَّرة 2026-09-03)

---

## 1) الوضع الحالي

| | **فرعك على غيتهاب** `backup/new-improvements-2026-08-28` | **خطي المحلي** (main هنا) |
|---|---|---|
| آخر كومت | `6f5ec83a` — 2026-09-02 03:20 UTC | `de6b514` — v19.7 — 2026-09-02 |
| النطاق الزمني | 2026-08-28 → 2026-09-02 (~41 كومت) | 2026-08-21 → 2026-09-02 (v12 → v19.7) |
| الملفات | 368 ملفاً (جذر الريبو = casino مباشرة) | 154 ملفاً تحت `platform/casino/` |
| القرابة | **لا سلف مشترك في git** — أرشيفان مستقلان من نفس الأصل (~19-21 أغسطس) |

> ⚠️ ملاحظة بنيوية: فرعك يضع ملفات الكازينو في **جذر الريبو**، وخطي يضعها تحت **`platform/casino/`**. الدمج الآلي بـ git merge مستحيل مباشرة (تاريخان منفصلان + بنيتان مختلفتان) — الدمج يجب أن يكون **ملفاً-ملفاً**.

---

## 2) ما يتفرد به فرعُك (يجب أخذه عند الدمج) ⬅️

| الميزة | الملفات | الكومتات |
|---|---|---|
| **أداة Provably Fair التفاعلية** (محقق HMAC-SHA256 داخل التطبيق + صفحة كاملة) | `provably-fair.html` (32.6KB مقابل 18.5KB)، `js/core/fair.js` (10.5KB — hmacSha256/roundSeed/outcome/verify) | `73f7e1ec` |
| **توافق الجوال** (رأس الصفحة، ثيم، أيقونات، ملء الشاشة للوحات) | `css/09-chrome.css` (37KB مقابل 22KB)، `css/12-chess.css` (14KB مقابل 5.9KB)، `css/10-dama.css`، `css/06-parchisi.css`، `css/04-games.css` (126KB مقابل 104KB) | `07b9a45a` + سلسلة 08-30/31 |
| **أصول ورق حقيقية v3** (52 ورقة webp بشعار المنصة) | `assets/cards/` — 58 ملفاً | `6f5ec83a`, `09919c19`, `066a7564` |
| **روليت بعجلة حقيقية مرقمة + دوران فيزيائي** | `js/games/engines.js` (روليت جديدة) | `19152b4a`, `066a7564` |
| **Crash+Keno جولات جماعية حية** | `server.js` (88KB مقابل 47KB!)، `engines.js` | `19152b4a` |
| **نموذج رسوم الغرف الجماعية** (100 كوينز/ساعة أو 5%/جولة) | `js/core/rooms.js` (55KB مقابل 38KB) | `bff2e1ec` |
| **دامة: قاعدة السوفلير** (النفخ عند رفض الأكل) | `js/games/dama.js` | `33a4cec8` |
| **شطرنج/دامة/بارشيسي: ملء اللوحات + تدوير** | `chess.js`, `dama.js`, `parchisi.js` | سلسلة 08-30 |
| **بارشيسي: أولوية هدية القتل/الدورة قبل قيم النرد** | `parchisi.js` | `785a8ebb` |
| **نظام أصدقاء** (DM + دعوة غرف) | `js/friends.js` (جديد كلياً) | — |
| **auth موسع** | `js/core/auth.js` (21.7KB مقابل 13.4KB) | — |
| **2FA محسنة** | `2fa.html` (20.8KB مقابل 17.8KB) | — |

## 3) ما يتفرد به خطي المحلي (يجب ألا يضيع) ➡️

| الميزة | الملفات |
|---|---|
| **البلياردو كاملاً** (5 أنواع: 8-ball WPA، بلاكبول EPA، **غولڤازور**، سنوكر، كاروم + فيزياء 240Hz + بوت خبير 5 أنماط + مؤقت 30-300ث) | `js/games/billiards*.js` ×3، `css/13-billiards.css`، 13 ملف اختبار — **غير موجود إطلاقاً في فرعك** |
| **رامي v19 كاملة** (قوانين السامبل الملزمة: فوجوك، مرموق، حرة/غير حرة، جزاء +51، عتبة 51 مع المسحوبة، تجاوز صارم لأعلى افتتاح، إدراج مصحح v19.6، تجميد الإنهاء 6ث v19.7) | `js/games/rami.js` (278KB مقابل 253KB) |
| **نافذة القواعد الموسعة ×4 لغات** | `js/rules/game-rules.js` (202KB مقابل 140KB) |
| **نظام الإحالة + أدوار الأدمنز v16/v17** | `server.js` (14 موضع referral)، `admins.html` (غير موجود في فرعك) |
| **كتالوج ألعاب موسع** (بلياردو + قواعد محدثة) | `js/games/catalog.js` (29KB مقابل 21KB) |
| **ترجمات أوسع** | `js/i18n/translations.js` (240KB مقابل 227KB) |
| مواصفات ووثائق | `GOLVAZOR_SPEC.md`, `HANDOFF_KIMI.md`, `BILLIARDS_PLAN.md` |

## 4) نقاط التعارض الحقيقية (تحتاج دمجاً يدوياً حذراً) ⚠️

| الملف | المشكلة | إستراتيجية الدمج المقترحة |
|---|---|---|
| **`server.js`** | فرعك: +Crash/Keno الحية +رسوم الغرف +أصدقاء (88KB). خطي: +الإحالة +أدوار الأدمنز (47KB). قاعدتان متباعدتان جداً | الأساس = نسختك (أشمل)، ثم زرع كتل الإحالة/الأدمنز الـ14 من نسختي + مسارات api الخاصة بها |
| **`js/games/rami.js`** | فرعك: أصول الورق الحقيقية في العرض. خطي: كل قوانين v19 + الخبير المحدث | الأساس = نسختي (القوانين أهم وأحدث)، ثم زرع دوال عرض الورق (getRamiCardHTML بأصول webp) من نسختك |
| **`js/games/engines.js`** | فرعك: روليت جديدة + كراش/كينو حية. خطي: تحديثات طفيفة (+208B فقط) | الأساس = نسختك، مع فحص فرق الـ208B عندي |
| **`index.html`** | فرعك: 55KB (صفحات/أقسام جديدة). خطي: 46.5KB (بلياردو + كاش v197) | الأساس = نسختك + إضافة سكربتات/أيقونات البلياردو + admins.html |
| **`js/core/rooms.js`** | فرعك: رسوم الغرف. خطي: تسجيل بلياردو في الغرف | الأساس = نسختك + كتلة تسجيل البلياردو |
| **`js/i18n/translations.js`** | إضافات متوازية (مفاتيح مختلفة في الغالب) | دمج مفاتيح: نسختي أساس + مفاتيح نسختك غير الموجودة عندي |
| **`js/games/catalog.js`, `main.js`, `css/04-games.css`** | إضافات متوازية | ملفاً-ملفاً: css نسختك أساس + كتلة البلياردو؛ catalog نسختي أساس + أي ألعاب جديدة عندك |
| **`js/games/dama.js`, `chess.js`, `parchisi.js`, `ronda.js`** | تحسيناتك أحدث وأشمل (سوفلير/ملء/تدوير) | الأساس = نسختك كاملة (خطي لم يعدلها جوهرياً بعد الانفصال) |
| **`js/rules/game-rules.js`** | نسختي أضخم بـ62KB (قواعد بلياردو+رامي v19) | الأساس = نسختي + أي بنود جديدة من نسختك (روليت/رسوم الغرف) |

## 5) ما نُفِّذ الآن (بدون أي مساس بفرعك)

- ✅ دُفع خطي المحلي كاملاً إلى فرع جديد منفصل: **`agent/billiards-rami-v19`**
- ✅ فرعك `backup/new-improvements-2026-08-28` لم يُلمس
- ✅ main لم يُلمس

## 6) خطوات الدمج المقترحة (عند موافقتك)

1. إنشاء فرع `merge/unified` من فرعك (الأحدث بنيةً وتوافقاً مع الجوال).
2. نقل حزمة البلياردو كاملة إليه (12 ملفاً + css + سكربتات index.html + كتلة rooms) — **صفر تعارض** لأنها ملفات جديدة كلياً عنده.
3. استبدال `rami.js` + `game-rules.js` بنسختي (قوانين v19)، ثم زرع دالة عرض الورق الحقيقي من نسختك في rami.js وتشغيل بطارية اختبارات الرامي (76+21).
4. زرع كتل الإحالة/الأدمنز الـ14 + `admins.html` في `server.js` نسختك.
5. دمج مفاتيح الترجمة والكتالوج.
6. بطارية الاختبارات الكاملة (بلياردو 323 + رامي 97 + بقية الألعاب) ثم عرض النتيجة عليك قبل أي دفع لـ main.

**الزمن التقديري للدمج الكامل مع الاختبارات: جلسة عمل واحدة مركزة.**
