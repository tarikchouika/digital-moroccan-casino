# GitHub Sync — v2.14 (هاتف الكات = مسؤول DB والإصلاحات والرفع؛ سام = مسؤول النشر الحي)

**تحديث 2026-09-12 (cat):** الفرع `arena/samsung-fixes-20260911` **مدفوع إلى GitHub** — commits: `a3aef4a` (frontend-tx) + `2087a2c` (v2.14 server). **اعتمادات مهيأة (token للمستخدم tarikchouika) — push يعمل من هذا الهاتف** مباشرة عبر HTTPS (`/root/.git-credentials`)، لم يعد هناك حاجة للـ patch/bundle اليدوي.

**أنا cat** (هاتف Samsung): قاعدة البيانات SQLite + الإصلاحات + `git push` إلى GitHub. **سام هو مسؤول النشر الحي** (Pages + خادم الإنتاج) — cat لا ينشر إطلاقاً.

## ماذا يجب أن يفعل sam لتفعيل v2.14 (خطوات ملزمة):

1. **جلب الفرع:** `git fetch origin && git checkout arena/samsung-fixes-20260911` (أو دمج/cherry-pick فوق فرعه العام). رأس الفرع الآن `2087a2c`.
2. **إعادة تشغيل خادمه** (pm2 casino-server أو ما يعادله لديه): الجداول الجديدة `transactions` و `bet_tickets` **تُخلق تلقائياً عند الإقلاع** بـ `CREATE TABLE IF NOT EXISTS` داخل `db.exec` في server.js — لا ترحيل يدوي ولا سكربت SQL مطلوب.
3. **نشر Pages من الشجرة المدمجة** (deploy-pages.sh أو ما يعادله): نقاط الواجهة الجديدة (شارات أنواع المعاملات، بحث التذاكر، تبويب «السجلات» للأدمن السوبر) **تعمل فقط بعد نشر sam** — قبل النشر ستواجه الواجهة الحية نقاط نهاية لا يفهمها الخادم القديم أو العكس، لكن لا شيء يتعطل: الواجهة تتسامح مع الغياب.
4. **التحقق بعد النشر:** دخول super → تبويب «السجلات» يعرض جدول المعاملات؛ لعبة أي لعبة → سجل التذاكر يعرض صفوفاً محفوظة بعد إعادة التحميل.

## محتوى v2.14 المدفوع (عقد الواجهة/الخادم):

- **جداول DB:** `transactions` (سجل معاملات دائم: user_id, type, amount, balance_after, counterparty_id/name, actor_id/name, game_id, note, created_at + فهارس) و `bet_tickets` (تذاكر رهانات دائمة: user_id, game_id, bet, won, payout, result_txt, created_at + فهرس).
- **POST /api/transfer:** يخصم من المرسل ويقيّد فعلياً للمستلم (UPDATE gold في DB للطرفين) ويسجل `transfer_out`/`transfer_in` عبر `logTx` — كانت الرصيد السابق لا يصل للمستلم إلا في الذاكرة أو لا يصل.
- **GET /api/transfers:** من جدول `transactions` — أنواع: transfer_out/in, charge, deduct, set_balance, referral_bonus, claim (limit 100 تنازلياً).
- **POST /api/rounds:** يسجل تذكرة في `bet_tickets`؛ **GET /api/rounds:** آخر 100 تذكرة للمستخدم من كل الألعاب؛ **GET /api/games/:gid/history:** تذاكر المستخدم للعبة (limit 25).
- **GET /api/admin/transactions** (سوبر فقط — أدمن عادي 403): فلاتر user_id/type/limit/offset، LEFT JOIN على users لجلب username (صفوف المستخدمين المحذوفين تبقى ظاهرة بـ username فارغ).
- **مسار الغرف (settle):** حُذف `transfersList` الذاكرية نهائياً واستُبدلت بـ `logTx` دائم: `win` للفائز و `bet` للخاسر مع game_id و balance_after.
- **الواجهة (commit a3aef4a):** renderTransactions متعدد الأنواع بشارات، دمج تذاكر الخادم/المحلي بلا تكرار + بحث، تبويب «السجلات» للسوبر + زر «سجل» لكل مستخدم في جدول الأدمن، مفاتيح ترجمة جديدة (ar/fr/en/دارجة).

## سجل v2.13 السابق (للمرجع فقط — كل هذا داخل الفرع المدفوع):

**التاريخ:** 2026-09-11 ~23:20 | **الفرع:** `arena/samsung-fixes-20260911` (كان commit `1185ded`)
**الحالة:** ✅ كان منشوراً حياً ومُتحققاً منه (Pages deploy `5c7e1d69`) + pm2 يعمل بالكود الجديد

## محتوى v2.13 (كلها أصلاً داخل الفرع المدفوع — للمرجع فقط):

### server.js (مهم — انسخها لخادم الكات إن كان هو الإنتاجي):
- `/api/health` صريح: `{"ok":true,"service":"dmgames-arena"}` — **مطلوب لسكربت النفق** (`/root/dmgames-tunnel.sh` يفحصه كل 30ث!)
- **fallthrough 404**: المسارات المجهولة تعيد 404 بدل `{ok:true}` الزائف
- **كوكي `SameSite=None; Secure`** في login/logout (وإلا يرفض المتصفح الجلسة عبر الووركر)
- جدول `settings` + `GET/POST /api/admin/rewards` (كانت تختفي — الآن تُحفظ في القاعدة)
- `GET /api/admin/stats/games` (تبويب المالية)

### الواجهة:
- **عجلة الحظ محذوفة كلياً** (طلب المستخدم): modal في index.html + engine في main.js + state.js claimDaily + i18n keys + CSS. زر الرصيد أصبح عرضاً فقط.
- **v2.12 لِمساعد الكات محفوظة بالكامل**: rdc4 ×9، الغرف الموحدة (5% / 1vs2 / 1vs3 / mode4)، rd-app، rd-renderer، rooms.js
- `live-ws-bridge.js`: **نسخة النفق** (isSSEMode — لا تستبدلها بنسخة الووركر السحابي!)

### بنية الإنتاج الآن (تحقّق قبل أي نشر جديد — النشر مسؤولية sam):
```
dmgames.pages.dev (5c7e1d69)
  → casino-phone.dmgames-api.workers.dev  (ووركر وسيط — KV: url)
  → نفق lhr.life (هاتف Samsung، localhost:3000)
  → pm2 casino-server = /root/dmgames-arena/server.js
  → data/royalcoin.db (42 مستخماً حقيقياً منقولة)
```

## ⚠️ تحذيرات التنسيق (لأي وكيل — والنشر الحي حكر على sam):

1. **لا تنشر فوق الحالة الحالية بدون جلبها أولاً** — نشر v2.12 طمر مؤقتاً v2.10/2.11 لأنه بني من شجرة قديمة. قبل أي نشر: `curl -sL https://dmgames.pages.dev/ | grep -c rdc4` يجب = 9، و `grep -c wheelModal` يجب = 0.
2. **سقطة KV**: هاتفانا يكتبان نفس مفتاح KV (نفق كل هاتف). حالياً Samsung يخدم الإنتاج. إن استلمت أنت KV، خادمك (40248) يحتاج نفس server.js المصلح (الأعلاه) — وإلا سترجع أعراض "لا حسابات / لا شحن" ثانية.
3. **راجع هذا الملف دائماً قبل البدء**: `CURRENT_TASK.md` في شجرة dmgames-arena — القسم 7 يوثق v2.13 كاملاً.

## v2.15 (cat — 2026-09-12) — إصلاحات القوائم + روندا لاندسكيب + بينالتي 9 جهات + بلاكجاك جماعي

**Commit:** `2bd0163` على `arena/samsung-fixes-20260911` (بعد `1d89380`)

### ما يجب أن يفعل sam:
1. `git fetch origin && git merge arena/samsung-fixes-20260911` (أو rebase فوق فرعه).
2. **إعادة تشغيل خادمه server.js** — تغييرات خادمية واحدة: `ROOM_GAMES_ALLOWED` يدعم bj الآن (سطر ~1699) + settleRound يقبل `w0-w3` (4 مقاعد) — لا ترحيل قاعدة بيانات (لا جداول جديدة هذه الجولة).
3. **نشر Pages** من الشجرة المدمجة. ملاحظة كاش: `css/14-ronda-classic.css` مذكور في index.html بـ `?v=rdc4` — **ارفع الرقم إلى rdc5** عند النشر لكسر كاش CSS (تغييرات لاندسكيب روندا كبيرة). كذلك يفضَّل رفع `?v=` لـ engines.js و blackjack.js و legal-ui.js و auth.js و utils.js (unified14 → unified15) — أو اعتماد كش Cloudflare تلقائي.
4. اختبار سريع بعد النشر: `curl -sL 'https://dmgames.pages.dev/' | grep -c 'wheelModal'` = 0، `grep -c 'badge" aria-label="22'` = 0، ولوحة roندا لاندسكيب: أيقونات اللاعبين بجانب أوراقهم والمقبض أعلى الوسط فوق العنوان.

### محتوى v2.15:
- **legal-ui.js**: حذف شارة «22» من بند الألعاب، توحيد أيقونة العدالة والشفافية (fa-solid fa-scale-balanced)، إضافة بند «العدالة والشفافية» للقائمة بعد Provably Fair، تثبيت side-foot أسفل القائمة على الجوال (07-responsive.css: sticky bottom).
- **auth.js/utils.js/auto-lang.js**: أزرار قائمة المستخدم بـ span data-i18n + إعادة بناء القائمة عند setLang + مراقب auto-lang يعيد بناءها بعد authRestore في الصفحات القانونية.
- **css/14-ronda-classic.css** (كتلة landscape جديدة في النهاية): توحيد حجم صفوف ظهر الخصوم (0.62/0.82 من chB)، مقاعد tl/tr/bl أفقية (أيقونة بجانب الأوراق: اليساريون أيقونتهم يمين الأوراق، tr أيقونته يسارها)، br الرئيسي على خط اليد أفقياً، رفع bl فوق مستوى الصف السفلي (فصل عن أوراق الرئيسي)، مقبض السجل أعلى الوسط فوق العنوان (rd-tophead ينزل clamp(16px,3.4vh,26px)).
- **engines.js Penalty**: 9 جهات (3×3: أعلى/وسط/أسفل × يسار/وسط/يمين) برموز أسهم، جدول PN_POS إحداثيات، مضاعف ×1.08 (RTP 96% — تصدي الحارس 1/9)، شبكة أزرار 3×3 (.pn-picks.nine)، مفاتيح ترجمة pn.tl..pn.br بالأربع لغات + pn.hint جديد.
- **blackjack.js BJMP**: بلاك جاك جماعي 2-4 لاعبين **بلا بانكر** — دوال نقية window.BJMP (newRound/applyAct/settle)، وضع الغرفة عبر rmove (سائق = driver) + bjmove/bjact/bjsettle، مهلة دور 30ث → stand تلقائي، بوت آلي (≥17 وقوف)، تسوية خادمية عبر settleRound بنفس مسار ضاما (w0-w3/draw — تعادل جماعي = استرجاع)، اختبار وحدوي tests/_bjmp_test.js (33/33).
- **server.js**: ROOM_GAMES_ALLOWED + bj، settleRound يقبل w0-w3.
- إصلاح جانبي: كتلة `#toasts` مكررة معلقة في css/03-components.css (من إصدار قديم) أزيلت — كانت تسبب اختلال أقواس.

### ملاحظات لـ sam (ثغرات موثقة — خارج نطاق cat هذه الجولة):
1. **blindResult لا يبثه server.js إطلاقاً**: وضع غرف penalty/rps على phone server معلق بعد اختيار الطرفين (اختيارات كشفها يصل مباشرة). يحتاج معالج action:'blind' يجمع الزوج ثم يبث blindResult — إن لم يكن خادمك السحابي (cf-worker) يعالجها فعلاً.
2. بقايا وصف قديم للبينالتي (×1.45/3 جهات) في catalog.js:560 و game-rules.js:1188-1223 — تحديث نصي فقط.
3. غرف البلاكجاك تعتمد rmove driver مثل روندا — الخادم يمررها كما هي (مدعوم أصلاً).

## v2.16 (cat — 2026-09-12) — ظهر بطاقات جديد شفاف + قرعة موزع الروندا + إزالة نهائية لعجلة الحظ

**Commit:** `c0da2a7` على `arena/samsung-fixes-20260911`

### لـ sam عند النشر:
1. دمج الفرع + **نشر Pages** — ملفات CSS تغيرت (14-ronda-classic/05-ronda/08-rami) — **ارفع أرقام الكاش**: rdc5 لـ 14-ronda-classic.css ورفع unified14→unified15 لملفات js/core أيضاً (state.js وmain.js تغيرا — العجلة أزيلت منهما نهائياً).
2. **إعادة تشغيل خادمه server.js إلزامي**: تغييرات خادمية مهمة — `/api/claim` يرد الآن **410 Gone** (عجلة الحظ أزيلت نهائياً)، `/api/me` بلا حقول claim، عمود `last_claim` حذف من CREATE TABLE (القواعد القديمة تحتفظ به بلا ضرر — CREATE TABLE IF NOT EXISTS لن يلمسها).
3. الأصول الجديدة في git: `assets/cards/back.webp` (RGBA شفاف 540×780 — **حل محل القديم**)، `back-sm.webp` (360×520 احتياطي للشاشات الصغيرة)، `back-new.png` (الأصل عالي الدقة). تحقق بعد النشر: `curl -s https://dmgames.pages.dev/assets/cards/back.webp` يجب أن يكون ~217KB وليس 241KB القديم.
4. اختبار بصري بعد النشر: ظهر البطاقات في كل ألعاب الورق (روندا/رامي/بلاكجاك/hi-lo/baccarat/داي-تشي/فيديو-بوكر/poker/andar-bahar) = التصميم الجديد بلا خلفية رمادية. لوحة الداشبورد بلا عجلة الحظ نهائياً — حتى زر الرصيد لم يعد يفتح شيئاً (عرض فقط).

### محتوى v2.16:
- **ظهر بطاقات جديد (صورة المستخدم)**: أزيلت الخلفية الرمادية بالكامل (0% متبقي، حواف سليمة). CSS: أزلنا التدرجات الداكنة المتراكبة التي كانت تحت الصورة القديمة المعتمة (14-ronda-classic .rd-back + 05-ronda .fd-card.back + 08-rami قاعدة سمة للبطاقات ذات الصورة). البلاكجاك وbaccarat وغيرها كانت شفافة أصلاً.
- **قرعة موزع الروندا (قاعدة «بينيا»)**: أول موزع يُختار بتوزيع ورقة واحدة مكشوفة لكل لاعب — أصغر ورقة (ترتيب 1<2<…<7<10<11<12) توزع. تعادل → إعادة سحب للمتعادلين فقط. حتمية كاملة من seed → الغرف متسقة تلقائياً بين العملاء. حدث `FirstDealerDrawn` يبث قبل أول CardsDealt مع بطاقات القرعة. معالج في rd-app.js (لوج + بانر + عرض ورقات القرعة) وapp.js القديم. اختبار وحدوي 13/13.
- **إزالة عجلة الحظ النهائية**: الخادم — `/api/claim` = 410، `/api/me` نظيف، `last_claim` حذف من الاستعلامات والمخطط؛ الواجهة — claimDaily حذفت من main.js وstate.js (كانت stubs toast)، مفاتيح ts.claim/ts.wait/ui.claim حذفت. ملاحظة: «ويل أوف فورتشن» (wf) لعبة حظ مستقلة — **ليست** العجلة اليومية وتبقى كما هي.

## v2.17 (2026-09-12 مساءً) — sam — رأس e41251b، نشر 29fb6db0

1. **اكتشاف**: كوميتات cat النهائية `c0da2a7`+`1c5bf47`+`f370166` لم تكن مدمجة في arena (دمج v2.16 شمل v2.14+v2.15 فقط — فحص merge-base). دُمجت كاملة بلا تعارضات: ظهر البطاقات الشفاف + قرعة بينيا 13/13 + إزالة العجلة النهائية (claim 410).
2. **روندا**: لاندسكيب — الرزمة بالهامش الأسفل مباشرة وسطاً (طلب حرفي)؛ بورتريه — الرزمة فوق مقبض السجل بلا تداخل (كانت مغطاة 100% باليد)؛ المقبض أعلى الشاشة فوق العنوان (طلب المستخدم الأول، قرار cat v2.15 مستعاد)؛ الخصم bl عاد لزاويته أسفل-يسار؛ القاعة المنهارة (0-15px) صارت 104px بنطاق صريح.
3. **قوائم قانونية**: حذف بند tx الميت (transactions.html غير موجودة) + بند fairness المكرر؛ ui.fair عادت "Provably Fair"؛ تبويب الإدارة display:none افتراضياً (admin/super فقط)؛ auth.js+api.js أُضيفت للصفحات الست الناقصة فأيقونة المستخدم عادت.
4. **إصلاح خادم**: ROOM_GAMES_ALLOWED سقط في حسم تعارض v2.16 → POST /api/rooms كان يرمي الخادم — استعيد سطر cat حرفياً (curl ok:true + probe 12/12).
5. **كاش**: rdc7 / unified16 / lu2.
6. **قيَم**: محلي 44+12+13+33+46 | حي 26/26 من dmgames.pages.dev | md5 مطابقة.
7. **لـ cat**: rami_sample_v19 = 74/78 (إخفاقات «دور الموالٍ» موجودة قبل تعديلاتي — من دمج فرعك؛ تحتاج جولتك).

## v2.18 (2026-09-12 ليلًا) — sam — رأس d3682ba، نشر 011e688b

1. **إصلاح جذري لقاعدة البرية في رامي السامبل (توضيح المالك النهائي)**: كان كوميتي السابق d9b9667 قد طبّق «منعاً كلياً» للبرية في المتتاليات — فهمٌ خاطئ. التوجيه الصحيح: البرية داخل المجموعة (متتالية أو متماثلة) تعمل **جوكراً فقط ولو في محل رقمها** → المجموعة تصير غير حرة لكنها ليست ممنوعة؛ حدّ جوكر واحد على الأكثر بأي نوع (بريتان معاً / برية+مطبوع = رفض)؛ لا يمكن لورقة العمل برقمها وجوكراً معاً (جذر حالة J♣-Q♣-Q♣). متتالية ببرية مسموحة في كل الأدوار (افتتاح/إدراج/إنهاء) بشرط القوانين.
2. **المحرك**: `_checkSequence` أعيدت كتابته (إلغاء المنع الكلي + إلغاء منطق الـ mask المزدوج القديم الذي سمح بـJ-Q-Q)؛ `isValidSet` كان يطبق المنطق الصحيح أصلاً (مؤكد بـ6 فحوص) — لم يتغير؛ `validateOpening`/`validateFinishStructure` يعاملان البرية كجوكر كامل (غير حرة) في المجموعتين بالتساوي — موحد أصلاً.
3. **الاختبارات**: أُعيدت الحالات الأربع الأصلية لحالة النجاح (كانت صحيحة أصلاً — أفسدتها أنا بفهمي الخاطئ في الجولة السابقة) + كتلة 8ح جديدة [SAMPEL-WILD] بـ16 فحصًا (حالة المستخدم J-Q-Q مرفوضة، البرية بمحل رقمها مقبولة، المواضع الثلاثة + الآس منخفض/مرتفع، الافتتاح/الإنهاء بالمتتالية-البرية يتطلبان نقية إضافية، الاستبدال يعمل، المتماثلة 6 فحوص، الطالاج لم يتغير). **100/100 + آس 21/21 + خبير: crashes=0 penalties=0 unfinished=0** + محاكاة 22.5k حركة صفر تجميد (الوضعان).
4. **كاش**: rami.js unified15→**unified17**.
5. **حارس ساعي (جديد)**: `/root/casino-work/hourly-guard.sh` — كورن جوب (id 01M2BVFD151C8CKFS2JBRRNAKD، كل ساعة د.17): fetch فرع arena → رأس جديد؟ → دمج → فحص صياغة JS/CSS → اختبارات node (رامي/قرعة/بلاكجاك) → نجاح كلها = نشر Pages + تحقق 200 → السجل في guard.log. أي فشل = توقف بلا نشر (لا إصلاح تلقائي).
6. **توثيق الجذور**: فشل رامي 74/78 السابق كان من صنعي أنا (d9b9667 + عدم تحديث اختبارات v19.2 المتعارضة) — ليس من cat. شُرح كاملاً في كوميت v2.18.


## v2.19 (2026-09-13 فجراً) — sam — رأس aff73c8، نشر 7f860814

1. **2FA — ثلاث مشاكل جذرية** (بلاغ المالك): (أ) QR محجوب بـCSP — أُضيف api.qrserver.com إلى img-src في _headers، مؤكد حياً: الصورة تُحمّل (naturalWidth=200). (ب) ازدواجية خانة الرمز — صندوق رمز الدخول twofaLoginBox كان ظاهراً دائماً مع حقل رمز التفعيل؛ الآن مخفي افتراضياً وshow2faLogin يقلب النافذة لوضع الدخول (إخفاء setup + زر التعطيل) وcloseTwofaModal يعيد الضبط. (ج) زر «تفعيل» لا يتغير رغم التفعيل — applyAuthUser يستدعي الآن _refresh2faStatus بعد authRestore. **دورة كاملة مثبتة عبر الواجهة برمز TOTP حقيقي (SHA1)**: تفعيل → reload → الزر مخفي والحالة ظاهرة → خروج → دخول → نافذة برمز واحد → دخول ناجح، صفر أخطاء.
2. **واتساب**: حُذف «واتساب 1» (212766672027) من contact؛ زر واحد «واتساب الرسمي» (212706865019) بأربع لغات. + **أيقونة عائمة #waFab** في كل الصفحات (index مباشرة + القانونية عبر legal-ui) تخفى في صفحات الألعاب بـCSS خالص (body.pg-game من nav) وفي ملء الشاشة. مؤكد حياً: elementFromPoint على مركزها = قابلة للنقر، وداخل لعبة display:none.
3. **النصائح حُذفت من نافذة القواعد في كل الألعاب**: كتلة العرض + CSS + **39 قسماً** من بيانات FULL_RULES (بموازنة أقواس python، صفر متبق) + مفتاح ui.tips الميت. جدول الأرباح/الهدف/الخطوات باقية (rulesBody للرامي 7590 حرفاً بلا 💡).
4. **كاش**: chrome2 / unified18 (auth + game-rules) / unified19 (translations) / lu3 (legal-ui).
5. **قيَم حية**: CSP يسمح qrserver | md5 مطابق للملفات الأربعة | 15/15 محلياً + دورة TOTP | انحدار: قرعة 13/13 + BJMP 33/33 + رامي 100/100.

## v2.20 (2026-09-13 صباحاً) — sam — رأس 0bec2f6، نشر 7d8b596b

**بلاغ المالك (تجميد الرامي + فقدان الجولة/الرهان عند الإغلاق)** — ثلاثة جذور:
1. الجولة كانت كائناً في RAM فقط: أي تحديث/إغلاق = ضياع كامل؛ SessionResume كان يحفظ «وسم جلسة» فقط بلا حالة. الرهان خُصم عند البدء لكن recordRound لا يشتغل إلا عند MATCH_END → الجولة الضائعة لا تُسجّل أبداً (الرهان يختفي صامتاً).
2. قفل RAMI_BUSY العالق عند دور البوت (مؤقتات مؤجلة بخلفية الهاتف).
3. **الحل الجذري [Persist]**: تسلسل حالة الجولة كاملة (ramiSerializeGame) في localStorage (rc_rami_active_round): اللاعبون (أيدي/مجموعات/نقاط/افتتاح/مرموق/فوجوك/لا تور/إقصاء) + الرoundManager (مجرف/مرموق/طاولة/مؤشر الجوكر/طور الدور/الشوط/المؤقت/أعلى افتتاح/شوط مضاعف/دورة الموزع/سجل الأشواط) + الإعدادات والرهان. **حفظ تلقائي بعد كل تقدم دور** (_processTurn) + عند البدء + بعد نهاية كل شوط. الاستئناف: ramiStartGame يجد جولة محفوظة مطابقة للإعدادات → يعيد بناءها كاملة **بلا خصم رهان ثانٍ** (gold ثابت — مثبت). إعدادات مختلفة = جولة جديدة صريحة (تُسجَّل القديمة كتذكرة خسارة [TicketSafety] — الرهان لا يضيع صامتاً أبداً بعد الآن). MATCH_END → مسح الحفظ بعد recordRound الطبيعي.
3. اختبارات: استمرارية e2e متصفح (بدء → 3 أدوار → **تحديث كامل** → استئناف بنفس الشوط/النقاط/الرهان 950/990) 7/7 حياً ومحلياً + تجمد e2e (busy عالقة → watchdog فكها وجبر البوت؛ جولة ضد بوتين 150+ حركة صفر تجميد) + انحدار كامل أخضر (100/100 + SAFETY crashes=0 unfinished=0 + 13/13 + 33/33). كاش unified20.
4. **يعمل user-vs-AI وuser-vs-user على نفس الجهاز** (الحالة الكاملة تستعاد للوضعين). الغرف الجماعية: تعتمد إعادة بناء الخادم (لم تتغير).
5. **دومينو + الطاولة (cat)**: غير مدفوعتين بعد على GitHub (فحصت الفرعين). الحارس الساعي سيلتقطهما عند الدفع ويختبرهما وينشرهما تلقائياً؛ سأتحقق يدوياً من القوانين والغرف عند وصولهما.

## v2.21 (2026-09-13 ظهراً) — sam — رأس df5608f، نشر b131c858

**طلب المالك: الرصيد وتوزيع الأرباح يقبل القيم العشرية 0.00** لدقة التوزيع.
- **اكتشاف حاسم**: SQLite يخزن REAL داخل أعمدة INTEGER **بلا تقريب** (أنواع ديناميكية — مؤكد بمحاولة مباشرة: gold=1050.55 typeof real) → لا حاجة لأي ترحيل جداول. المشكلة كلها كانت في **التقريب داخل الكود**.
- **server.js** (15 تعديلاً + دالة r2): logTx يخزن القيم الدقيقة (كان Math.round)، كينو/كراش payout بـr2 (كان floor)، التحويل/الشحن/الخصم/تعديل الرصيد/المكافآت r2(Number)، رهانات ke/av تقبل عشرياً (كانت ترفض Number.isInteger)، رسوم الغرف 5% عشرية دقيقة (كانت Math.round)، قسمة الفرق العادلة r2 (كانت floor+remainder)، payout/net في الاستجابات نظيفة (لا مزيد من 10.020000000000001).
- **الواجهة**: state.js كان يقطع العشري صامتاً (parseInt 999.75→999!) صار r2num؛ fmt يعرض منزلتين عند الكسور (1.000,50 / 0,01 / 77,70)؛ GB العام ورهان رامي وحقول التحويل step=0.01 min=0.01 inputmode=decimal؛ sendCoins parseFloat.
- **قيَم حية**: شحن 10.55 → 100010.55 ✓ | تحويل 5.25 → 100005.3 وتذكرة معاملة {amount:5.25} ✓ | رهان كينو 7.75 ✓ | تسوية غرفة 10.55: رسم 0.53 ورابح +10.02 → 99999.47 ✓ | تسوية 7.77: رسم 0.39 payout 7.38 نظيفة ✓ | fmt حياً: 1000.5→«1.000,50» ورصيد 999.75 يعيش save/load ✓ | انحدار 13/13+33/33+100/100+21/21 ✓ | كاش unified21 (5 ملفات) md5 حياً مطابق.
- **لـ cat**: خادم النفق يحتاج git pull (كل التسوية تصبح عشرية). ووركر السحابة D1 (cf-worker/worker.js:597 Math.round وparseInt) يحتاج نفس المعالجة إن صار هو مسار التسوية النشط.

## v2.20 (2026-09-13) — cat — الطاولة 🎲 + الضومنة 🁫: لعبتان جديدتان مدمجتان بمعمارية المشاريع المستقلة

> الوكيل cat. مبني فوق دمج كامل لفرع arena حتى رأس sam `aff73c8` (v2.19) — رامي 100/100 وقرعة 13/13 وBJMP 33/33 وv217 46/46 وv219 15/15 كلها خضراء بعد الدمج. **sam ينشر + يعيد تشغيل خادمه.**

### ما هذا: مشروعان مستقلان محكومان بعقد المنصة (نمط ronda-game)

- **الطاولة (bg)** — `backgammon-game/`: محرك backgammon حتمي كامل القواعد (افتتاح بنردَين، ضرب/حاجز، إدخال إجباري، إخراج، مارس/باك جامون، مباريات 1/3/5) + ذكاء ثلاثي المستويات (تعداد تتابعات + تقييم + رد خصم للمحترف) + واجهة «جوز فاخر» ببادئة bw-.
- **الضومنة (do)** — `dominoes-game/`: Draw Dominoes Double-Six حتمي (28 قطعة، سحب كلاسيكي/Block، البادئ بأعلى دبل إلزامي، القطعة المسحوبة إلزامية، انسداد بالتمرير المزدوج، أول لهدف 50/100/150/200) + ذكاء ثلاثي المستويات + واجهة «قهاوة مغربية» ببادئة dm-.
- عقد الدمج نفسه المعروف: `eBackgammon(g)/eDominoes(g)` يبنيان المسرح، `initBackgammon()/initDominoes()` يربطان، `cleanupBackgammon()/cleanupDominoes()` ينظفان عند الخروج (مستدعيان من closeGamePage).

### إصلاحات جوهرية أثناء التدقيق (مشروعا اللعبة كانا يظنان المنصة غائبة):

1. **المحفظة**: كانا يستدعيان `root.take()/root.give()` غير الموجودين → وضع المنصة كان يسقط لمحفظة محلية وهمية (500) ويتجاهل رصيد اللاعب! أصبحا `takeBet()/giveWin()` الحقيقيين (العقد الرسمي state.js) — الرهان يُخصم من ST.gold، الفوز يضاعف (×1.5 مبتدئ/×2 متوسط/×3 محترف).
2. **تذاكر السجل**: `recordRound(won, payout, txt, bet, 'bg'/'do')` للفوز والخسارة والانسحاب — كانت غائبة كلياً في الضومنة.
3. **خطأ حقيقي في الطاولة**: نقاط كل جولة كانت تُحسب مرتين (aiStep كان يستدعي checkGameEnd بعد حسم move) — مباراة هدف 1 كانت تنتهي 6:4. أُصلح + موثق في backgammon-game/INTEGRATION.md.
4. **RTL في الضومنة**: تلميحات طرفي السلسلة كانت على الجهة المعاكسة بالعربية — أُصلحت وفق direction.
5. **الكتم**: مزامنة `ST.mute` عند attach وكل refresh (زر السماعة بالهيدر يتبع لحظياً).
6. **CSS**: مسرحا اللعبتين `height:100%` بدل 100vh/dvh (يمنع القص مع fitGameStage) + نقوش نرد الشعار (bp5/bp6) كانت بلا CSS.

### الربط المركزي (ملفات المنصة):

- `catalog.js`: 45 لعبة الآن — مدخلان bg/do بقسم التقليدية بعد blca + RULES مختصرة عربية لكل منهما. **featured صار بالمعرف** (findIndex 'wf') لأن الإدراج أزاح GAMES[17].
- `main.js`: GAME_IMG (bg→backgammon, do→dominoes) + initFor (backgammon/dominoes) + cleanupBackgammon/cleanupDominoes في closeGamePage.
- `engines.js`: getterان كسولان (نمط billiards) — `ENG.backgammon/ENG.dominoes` يقرآن window.eBackgammon/eDominoes.
- `index.html`: 16 وسم script + وسمي link للـ CSS — كلها بنسخة `?v=bgdo1`.
- `translations.js`: 103 مفاتيح (bg=50، dm=53) منسوخة حرفياً بأربع لغات — لا تصادم مع dama.*.
- `game-rules.js`: FULL_RULES.bg/do بأربع لغات كاملة (بلا tips — حُذفت من المنصة كلها في v2.19).
- **rooms.js لم يتغير**: اللعبتان محليتان فقط (AI + جهاز واحد) هذه الجولة — بلا معالجات غرف؛ إضافتهما لـ roomGameIds كانت ستعرضهما معطلين في منشئ الغرف.
- `scripts/deploy-pages.sh`: ينقل backgammon-game + dominoes-game، يحذف INTEGRATION.md من النشر.

### الأصول: أيقونات وخلفيات بهوية المنصة (ذهبي #d9a94e على خشب داكن):

- `assets/games/backgammon/`: icon.webp 512×512 (رقعة جوز بأوتاد + حجران + نرد) + background.webp 1376×768.
- `assets/games/dominoes/`: icon.webp 512×512 (قطعتا دومينو عاجيتان بنقاط نحاسية) + background.webp.

### الاختبارات (كلها على خادم 4173 DM_TEST_MODE=1):

- `tests/_bg_do_integration_test.js` **30/30** (ثلاث تشغيلات ثابتة): فتح اللعبتين، مباراة طاولة (خصم+رقعة 24 نقطة+سير حي)، مباراة ضومنة (يد 7 قطع+خصم)، انسحاب بتذكرة خسارة، خروج وإعادة فتح، بطاقتا الكتالوج بالأيقونات (naturalWidth 512)، الفرنسية عبر TR، صفر أخطاء كونسول.
- `tests/_bg_do_gameplay_test.js` **18/18** (تشغيلات متعددة): **مباريات كاملة حقيقية** من الافتتاح حتى matchEnd عبر واجهات المستخدم نفسها (rollClick/doMove/passTurn للطاولة؛ pickHand/pickEnd/tryDraw/tryPass/الجولة التالية للضومنة) — التسوية المالية دقيقة (فوز ×2 → 1000−25+50=1025 ✓) + تذاكر recordRound صحيحة الحقول.
- انحدار كامل بعد الدمج: رامي 100/100 + قرعة 13/13 + BJMP 33/33 + v217 46/46 + v219 15/15.
- إصلاح جانبي: `tests/_rd_pw.js` — ثنائي @sparticuz/chromium مبني x86-64 فقط (الجهاز aarch64) → سقوط تلقائي لثنائي Playwright المحلي + locale ar-MA (كشف لغة v2.16.1 غيّر عناوين صفحات الزائر بلا تفضيل محفوظ — الاختبارات كانت تتوقع العربية الثابتة).

### لـ sam (خطوات ملزمة):

1. `git fetch` + دمج هذا الفرع — ثم **نشر Pages** (deploy-pages.sh يحمل المشروعين الجديدين تلقائياً).
2. **لا تغيير خادم مطلوب** — اللعبتان واجهة صرفة (الرصيد محلي ST.gold + تذاكر /api/rounds الموجودة). إعادة تشغيل الخادم غير ضرورية هذه الجولة (لكنها لا تضر).
3. تحقق سريع بعد النشر: صفحة الألعاب → 45 بطاقة → «الطاولة» و«الضومنة» بقسم التقليدية → افتح كل لعبة → مباراة برهان.
4. الحارس الساعي سيلتقطها تلقائياً عند رفع الفرع — اختباراته (رامي/قرعة/بلاكجاك) لا تتأثر بهذه الجولة.



## v2.22 (2026-09-13 عصراً) — sam — دمج لعبتَي cat (الطاولة + الضومنة) ونشرهما — رأس الدمج 78957d6، نشر d8051746

**سبب تأخر النشر (سؤال المالك)**: cat دفع إلى فرعه `arena/samsung-fixes-20260911` (رأس 2f7e297) وليس إلى فرع arena الذي يراقبه الحارس الساعي — فرؤى «لا جديد» مرتين. **أصلحت الحارس الآن**: يراقب الفرعين معاً (arena + samsung-fixes) ويدمج الأحدث منهما.

**الدمج**: 2f7e297 فوق arena (تعارض توثيقي واحد في GITHUB_SYNC حُل بالدمج الكامل للقسمين). اللعبتان محليتان بالكامل (عقد المشاريع المستقلة مثل روندا كلاسيك) — **لا تغيير خادم**.

**تحققي المستقل فوق دمجي (طلب المالك: قوانين + غرف)**:
- **قوانين الطاولة 12/12** (تدقيق مباشر للمحرك): توزيع 15+15، اتجاهان متعاكسان، الحاجز بإدخال 24−نرد إلزامي، الضرب يرسل الخصم للشريط، منع الهبوط على حجرين، لا إخراج قبل اكتمال البيت، الإخراج بقواعده، الحتمية، checkGameEnd (مارس/باك جامون)، **pip الافتتاح = 167 لكل لاعب** (مطابق للتوزيع الرسمي).
- **قوانين الضومنة 15/15**: 28 قطعة فريدة، يد 7×7 + boneyard 14، الافتتاح الإلزامي بدبل 6-6 (تقتضيه الضومنة المغربية)، المطابقة بالأطراب فقط، رفض قطعة غريبة وخارج الدور، الحتمية.
- **اختبارات cat أعيدت فوق دمجي**: لعب عميق 18/18 (×3 تشغيلات متتالية — فشل واحد عابر أول مرة بسباق توقيت مع الخادم المُعاد إقلاعه، ثم 18/18 + 18/18 + 18/18) — خصم رهان حقيقي 1000→975، تسوية ×2 → 1025، تذاكر recordRound صحيحة الحقول (bg/do، bet، won، payout). تكامل 30/30.
- **الغرف**: اللعبتان غير مفعّلتين في نظام الغرف هذه الجولة (تصميم cat الموثق). سلوك نظيف مؤكد: زر «العب مع صديق» الرسمي (#roomBtn) **مخفي تماماً** لهما عبر syncBtn، وtoggleFromGame يعود صامتاً. اللعب المحلي: ضد الحاسوب (3 مستويات ذكاء) أو لاعبين على نفس الجهاز.
- **انحدار المنصة فوق الدمج**: قرعة 13/13 + بلاكجاك 33/33 + رامي 100/100 + فحص v2.17 شامل 46/46 — لا شيء انكسر فوق تحسيناتنا (العشرية + الاستمرارية + كل ما سبق).
- **حياً من dmgames.pages.dev**: 18 ملف اللعبتين (محركات/أصول/CSS بوسوم bgdo1) كلها 200 + 45 لعبة بالكتالوج + المحركان محمّلان + صفر أخطاء JS.

## v2.21 (2026-09-13) — إعادة تسمية المنصة: DTSG — Digital Traditional Skills Games (نطاق server-docs)

> تغيير الهوية الكاملة للمنصة: «Digital Moroccan Casino» → **DTSG — Digital Traditional Skills Games** («ألعاب الدكاء التقليدية الرقمية»). الأصول الجديدة في `assets/dtsg/` (شعار/بانرات/خلفيات/favicon). هذا القسم يوثق تغييرات نطاق server-docs فقط — الواجهة (HTML/CSS/JS) تعالجها جولة الواجهة.

### ما تغير في server.js (سطران فقط):
1. **مُصدر TOTP في /api/2fa/enable**: `DigitalMoroccanCasino` → `DTSG` (مصدر التطبيق في تطبيقات المصادقة Authenticator — ظاهر للمستخدم). التغيير يُفعَّل **بعد إعادة تشغيل الخادم** — لا أثر على الرموز الموجودة (secret لكل مستخدم لا يتغير، فقط تسمية المُصدر عند تفعيل 2FA جديد).
2. **رسالة الإقلاع**: `Digital Moroccan Casino Live Server` → `DTSG (Digital Traditional Skills Games) Live Server`.

### ما تغير في الحزم:
- `package.json` + `package-lock.json`: name → `digital-traditional-skills-games`، والوصف محدث بلا «كازينو» — الاسم لا يُستخدم كي لا يُعاد التثبيت، لا يكسر شيئاً.

### الوثائق:
- `HANDOFF_KIMI.md`: العنوان + سطر الاسم محدثان للاسم الجديد (AR+EN).
- `GITHUB_SYNC.md`: «لعبة كازينو مستقلة» → «لعبة حظ مستقلة» (وصف ويل أوف فورتشن — الاسم الجديد لا يتضمن كازينو).

### ما لم يُمس (عمداً — ليست أسماء علامة):
- `royalcoin.db` و `royalcoin` في التعليقات (اسم ملف قاعدة البيانات الفعلي على القرص).
- `pm2 casino-server`، ووركرات `casino-phone/casino-api.dmgames-api.workers.dev`، مسار `/root/digital-moroccan-casino/` القديم، فرع git `arena/01a081af-digital-moroccan-casino` (بنية تحتية للنشر والنفق).
- `'Moroccan Ronda'` (server.js:553) — اسم لعبة.
- `SEC-2026-DMC-001` في `_headers` — معرف تقرير تدقيق أمني تاريخي.
- `data/royalcoin.db`، كوكي `sid`، `/api/health` service `dmgames-arena` — عقد تشغيلي لا يُمس.

### تحقق cat (E2E على منفذ 4321 DM_TEST_MODE=1):
- `node --check server.js` ✓ — JSON الحزم ✓
- إقلاع: رسالة DTSG ✓، `/api/health` ok ✓
- تسجيل مستخدم مؤقت + `/api/2fa/enable` → `issuer=DTSG` ✓ — ثم حُذف المستخدم المؤقت (القاعدة عادت لـ42 مستخدماً) وحُذفت جولات group_rounds اليتيمة من إقلاع الاختبار (لا رهانات عليها).

### لـ sam: إعادة تشغيل الخادم كافية (لا ترحيل DB — التغيير سلاسل عرض فقط) + نشر Pages مع جولة الواجهة.

### تكملة v2.21 — نطاق الواجهة الكامل (cat):

**الأصول المثبتة في `assets/dtsg/`** (من حزمة هوية المالك — بلا توليد):
- `dtsg-logo-main.webp` (638×701 شفاف) + `dtsg-logo-watermark.png` (540×540 — بديل logo-transparent في العلامات المائية داخل كل الألعاب) + `coin-obverse/coin-reverse.webp` (وجه الأسد/ضهر الشعار — عملة القلب cf صارت تستخدمهما بدل chip-gold/chip-red)
- بانرات: `dtsg-banner-hero-{dark,light}.webp` (الرئيسية — تتبدل تلقائياً مع الثيم) + `dtsg-banner-scene-{dark,light}.webp`
- خلفيات: `dtsg-bg-page-{dark,light}.webp` (خلفية الجسم بكل الصفحات — بديل dark-bg/luminous-background) + `dtsg-bg-tile-dark.webp`
- فافيكونات: `assets/dtsg/favicon/dtsg-{16,32,48,64,128,180,192,256,512}.png` + `dtsg.ico` — كلها شفافة من اللوغو

**لوحة الألوان الجديدة** (css/01-variables.css):
- `--gold: #FFD23F → #d9b45c` · `--gold2: #F5C518 → #f2d489` · `--bg: #070B12 → #0b1526` (كحلي زليج) · `--t1: #F6F8FC → #e9e2cf` (كريمي) · copper → `#b8892f/#a3742a`
- radiant: `--bg: #FBF9F4 → #f7f1e2` (عاجي كريمي) + الذهبي الداكن للنصوص `#8a6420`
- كل الـ rgba(255,210,63,*) في الـ 14 ملف CSS → rgba(217,180,92,*) والدرجات المتممة حُسبت نسبياً

**الاسم في كل مكان**:
- العناوين الستة: `<title>` لكل الصفحات التسع = «ألعاب الدكاء التقليدية الرقمية» + og:title/meta description + theme-color → #0b1526
- الشريط الجانبي: اللوغو الجديد + `<b>DTSG</b> ألعاب الدكاء التقليدية الرقمية` (index.html + legal-ui.js للصفحات الثمان)
- التذييل: «DTSG — ألعاب الدكاء التقليدية الرقمية»
- الترجمات: `ui.brand`، `a.intro`، `c.intro`، `p.intro`، `t.intro`، `ui.heroSub`، `legal.about.p1`، `legal.about.title`، `legal.*Desc` بالأربع لغات — صفر «Digital Moroccan Casino» متبقية
- `ui.casino` (تسمية فلتر تصنيف): «كازينو» → «الطاولة والورق / Tables & Cards» (المعرف الوظيفي `cat:'casino'` في catalog لم يتغير — متوافق مع filterG)
- رؤوس ملفات JS الثلاثين + رسائل الكونسول (main.js): كلها DTSG
- server.js: مُصدر TOTP → DTSG + رسالة الإقلاع (في نطاق server-docs أعلاه)

**كاش**: `?v=dtsg1` على كل CSS/JS المعاد تسميته في index.html + الصفحات القانونية الثمان (01-13 CSS، translations، main، legal-ui، auto-lang، core/*)

**التحقق (cat — على الخادمين 3000 و4173)**:
- **فحص المتصفح 56/56**: index (title/brand/foot/favicon/بانر داكن/خلفية/ذهب/كحلي/صفر أخطاء) + radiant (عاجي + البانر المضيء + الخلفية الفاتحة) + الصفحات القانونية الثمان ×5 فحوص (title/favicon/لوغو/لا علامة قديمة/صفر أخطاء)
- **أصول داخل الألعاب 5/5**: علامة glogo-wm المائية = dtsg-logo-watermark (كل ألعاب gFrame) + عملة القلب وجهان الأسد/الشعار + بلياردو لوغو الطاولة dtsg
- **انحدار كامل**: رامي 100/100 + قرعة 13/13 + BJMP 33/33 + BGDO 30/30 + v217 46/46 + v219 15/15
- **مسح نهائي**: صفر «Moroccan Casino/كازينو المغرب الرقمي» في أي نص ظاهر أو أصل مرجعي (البقايا: هامش الكازينو الرياضي في تعليقات RTP = مصطلح house edge، وتصنيف catalog للألعاب العالمية — مقصودان)

### لـ sam: نشر Pages + إعادة تشغيل الخادم (إصدار TOTP DTSG). أصول dtsg كلها في git. الاسم القديم لم يعد يظهر للمستخدم في أي مكان.

## v2.22 (2026-09-13) — نطاق timer-layout: مؤقت الدور بجانب أيقونة اللاعب (ضاما + الشطرنج)

> طلب المالك: «مؤقت اللعب في لعبة ضاما والشطرنج يظهر محشوراً تحت أيقونة اللاعب — يجب وضع المؤقت في جنب أيقونة اللاعب بشكل واضح لا يحشر مع أو وراء عنصر آخر» (مثل ما تم مع روندا).

### التغيير (نمط روندا rd-turn-timer حرفياً — كبسولة ذهبية تبرز من حافة أيقونة صاحب الدور):

- **`js/games/dama.js`**: عنصرا شارة `<span class="dama-ptimer" id="damaOppTimer|damaMainTimer" hidden>` داخل أيقونتي الخصم/اللاعب (dama-picon). دالة جديدة `damaPaintSeatTimers(txt, whose)` ترسم العدّاد على أيقونة صاحب الدور الحالي (me/opp) وتخفي الأخرى + حالة low (≤10ث) حمراء نابضة. `damaRenderTimer` يكتب النص في العنصر القديم (#damaTimer — للتوافق) ويرسم الشارة.
- **`js/games/chess.js`**: نفس العقد — شارتا `ch-ptimer` (chessTopTimer/chessBotTimer) داخل أيقونتي ♚/♔. الأيقونات تمثّل الألوان دائماً (أعلى=أسود، أسفل=أبيض) في النمطين وبلا التفات لقلب اللوحة: الشارة تظهر على أيقونة لون صاحب الدور (w→أسفل ♔، b→أعلى ♚).
- **إصلاح توقيت حقيقي (كان في الأساس قبل هذا التغيير)**: `damaStartTimer`/`chessStartTimer` كانا يستدعيان الرسم الأول **قبل** إنشاء `_turnTi` فيظهر النص فارغاً لأول ثانية (⏱ يظهر فقط من أول tick). أُعيد الترتيب: إنشاء المؤقّت ثم الرسم — الشارة تظهر فور بدء الدور.
- **`css/10-dama.css` + `css/12-chess.css`**: `.dama-ptimer`/`.ch-ptimer` — كبسولة ذهبية (`linear-gradient(#ffd75e,#d9b45c)`، حد أبيض، tabular-nums) بمنزلة absolute `top:-8px; inset-inline-end:-14px` (منطقية: تتبع اتجاه اللغة ar/en تلقائياً) — نفس قيم #rdStage .rd-turn-timer. حالة low حمراء بنبض أسرع. عنصرا النص القديمان (#damaTimer/#chessTimer) يُخفيان بصرياً (display:none) — يبقيان في DOM لكتابة النص (توافق اختبار tests/_dama_fixes_test.js:52 الذي يقرأ ⏱ من #damaTimer).
- **`index.html`**: رفع كاش `?v=tl1` على الملفات الأربعة.

### ما لم يُمس (حفاظاً على المكتسبات):
- منطق المؤقت نفسه (مهلة، حركة آلية عند الانتهاء، مؤقت الغرف على دور صاحبه فقط) — بلا تغيير سلوكي.
- تخطيطات البورتريه/اللاندسكيه القائمة (P-Seats/Landscape) — الشارة مطلقة على الأيقونة فلا تُزاح أي عناصر.
- روندا/بارشيسي/الرامي — عرضها الأصلي (روندا أصلاً هي المرجع).

### التحقق (خادم اختبار 4321 DM_TEST_MODE=1):
- `tests/_timer_seat_probe_test.js` (جديد) **25/25**: ظهور الشارة على أيقونة صاحب الدور + إخفاء أيقونة الآخر + الهندسة (تتزحلق على حافة الأيقونة وتبرز ~14px — عقد روندا المقاس) + داخل الشاشة بلا قصّ، في البورتريه واللاندسكيه للعبتين + اختبارات الدورة الكاملة (حركة آلية → دور الآلي بلا شارة → عودة الشارة لدوري) + قلب لوحة الشطرنج (autoFlip → شارة الأسود على ♚) + صفر أخطاء صفحة.
- انحدار: `tests/_dama_fixes_test.js` **15/15** (يشمل فحص ⏱ في #damaTimer + الحركة الآلية) + `_chess_browser_test` نفس نتيجة HEAD بالضبط (31✓ + 4 فشائل سابقة: bet chips رُفعت من الإعداد بجولة أقدم، stake label شارة غير موجودة أصلاً، MP new-match — كلها موجودة على HEAD قبل هذا النطاق ومطابقة بالعدد).
- `node --check` على الملفات المعدلة ✓.

### ملاحظة للسرب المتوازي: هذا النطاق يخص ضاما/الشطرنج فقط. ملفات غرف الطاولة/الضومنة (bg-room/domino-room) ومستوى الخبير عند وكيل آخر — لا تداخل (تحقق grep: لا استخدام لعناصري في ملفاتهم).

## v2.22 — نطاق ai-expert (توحيد مستوى الخبراء في كل الألعاب)

> نطاق الذكاء الاصطناعي: بوت الخبير في ضاما/الشطرنج/الطاولة/الضومنة بمستوى الخبير وأعلى قوة ممكنة. لا تداخل مع نطاقات الغرف/التنسيق (ملفاتي: المحركان + chess.js/dama.js في مسار الذكاء فقط + القواميس للتسمية).

### الطاولة `backgammon-game/js/engine/bg-game.js` — خطأ جوهري أُصلح + قوة:
1. **الخبير كان مقلوب الإشارة**: `total = ev + replyV*0.45` تجمع أفضل ردّ للخصم بدل طرحه — كان يختار عمداً المواضع الأضعف أمام ردود الخصم. مقاسة بالبذور: خبير ضد متوسط **0-8** خسارة كاملة (المستوى الأعلى كان أضعف من الأوسط!). بعد الإصلاح: **10-4** (والاتجاه العكسي 7-7 → 8-4 للخبير كخصم).
2. **توقع رياضي حقيقي**: ردّ الخصم يُحسب عبر الرميات الـ21 كلها موزونةً باحتمالها (دبل 1/36 · غير مزدوج 2/36) بدل رمية عشوائية واحدة — مع ميزانية داخلية (2200ms) بمعايرة تجعل أسوأ افتتاح (3-3) ~850ms.
3. **حتمية كاملة للخبير**: صفر Math.random في المسار — نفس الموقع + نفس النرد = نفس الحركة (مؤكد ببذور مختلفة).
4. **كتاب افتتاح**: الرميات الست المؤكدة نظرياً (2-1/3-1/4-2/5-3/6-1/6-5) من جدول — 3-1 يصنع نقطة الخمسة في 6ms (كان ~800ms بحث).
5. **تقييم أثرى**: صفوف نقاط البيت المتتالية تُقيّم تربيعياً + في السباق البحت كفاءة إخراج (الأحجار الخارجة أولاً + عقوبة حشو 5+ على نقطة).
6. الغرفة/الوضع المحلي بلا تغيير — فقط الذكاء.

### الضومنة `dominoes-game/js/engine/domino-game.js` — عدالة + قوة:
1. **إصلاح غش معلوماتي**: الخبير (المستوى 2) كان يقرأ **يد الخصم الحقيقية** (`s.hands[opp]`) لتقدير ردود الخصم — رؤية مخفية. الآن «غير المرئي» = كل ما ليس في يد الذكاء نفسه أو على السلسلة (يد الخصم والبنك معاً غير مرئيين) — عدالة كاملة، والقوة عبر الاحتمالات لا الكشف.
2. تقديرات جديدة: احتمال انسداد الخصم (حصة غير المرئي بلا رد على الطرفين) × وزن قرب الإفراغ + مكافأة متابعة الطرف المفتوح + وعي هامش الهدف.
3. القياسات (مباريات كاملة ببذور متعددة): خبير ضد متوسط ~63% · خبير ضد مبتدئ ~90% · تدرج نظيف بين كل زوج مستويات · fuzz 60 مباراة بكل الأزواج: صفر أخطاء وكلها تصل matchEnd.

### الشطرنج `js/games/chess.js` — ميزانية حقيقية + سرعة:
1. **الميزانية كانت وهمية**: فحص الوقت بين تكرارات العمق فقط — تكرار عمق 9 واحد كان يتجاوزها (قِيست خطوات 20-26ث على هذا الجهاز). الآن المهلة تُفحص **داخل البحث** (كل 64 عقدة + عند كل حركة جذر): أسوأ خطوة ≈ الميزانية + نافذة GC واحدة.
2. **`chessCloneSearch`**: نسخة خفيفة للبحث — البحث كان ينسخ log/captured/rep في كل عقدة (مخصصات هائلة × ملايين العقد = ضغط GC هو مصدر التجاوزات). البحث يقرأ الرقعة/الدور/الحقوق فقط؛ اللعب الحقيقي يظل بالنسخة الكاملة (تكرار المواقع والسجل محفوظان للعرض والحكم). حارسة `else if (s.rep)` في chessMakeMove للنسخ الخفيفة (تكرار المواقع يُحكم في اللعب الحقيقي فقط — كما هو مقصود للبحث).
3. **سلامة مؤكدة بأرقام مرجعية**: perft(3)=8902 · perft(4)=197281 (مطابقة تماماً).
4. سلوك اختيار الحركة في المستويات الأدنى بلا تغيير؛ البوت في الغرف كما كان (الآن يحترم ميزانيته فعلاً).

### ضاما `js/games/dama.js` — ميزانية حقيقية:
- نفس إصلاح المهلة داخل البحث (فحص كل 64 عقدة بمهلة 92% من الميزانية + احتياط عمق 1 عند الميزانية الشبه صفرية). الخبير (عمق 16/5ث) كانت شملاته تتجاوز 10ث — الآن أسوأ شملة ≈ الميزانية. منطق التقييم والبحث بلا تغيير إطلاقاً (المكتسبات محفوظة).

### توحيد التسمية «خبير» (كانت الطاولة/الضومنة «محترف» بينما ضاما/الشطرنج «خبير»):
- `js/i18n/translations.js` (bg.level.2 + dm.level.2 — عربي/دارجة «خبير») + `bg-i18n.js`/`domino-i18n.js` + أزرار `bg-html.js`/`domino-html.js` + جداول payouts في `js/rules/game-rules.js` (4 لغات × لعبتان). المضاعفات الاقتصادية (×1.5/×2/×3) لم تتغير.

### الاختبارات:
- **جديد `tests/_ai_expert_test.js`** (**26/26 أخضر**): هيمنة كل أزواج المستويات بالمحركين + صفر حركات غير قانونية + عدالة الضومنة (فحص آلي أن `_evalMove` لا يقرأ يد الخصم) + حتمية خبير الطاولة عبر بذور مختلفة + كتاب الافتتاح (نقطة الخمسة فورية) + ميزانيات الوقت الأربع + perft الشطرنج + تسمية «خبير» في كل القواميس.
- انحدارات: قرعة روندا 13/13 · BJMP 33/33 · رامي آس 21/21 · رامي سامبل v19 100/100 · رامي خبير exit 0 · v219 15/15 · BGDO تكامل 30/30 · BGDO لعب كامل 18/18 (أُصلح الاختبار نفسه: تعيين المستوى صراحة قبل البدء) · node --check على كل معدل ✓.

### ملاحظة للسرب المتوازي: ملفات هذا النطاق: المحركان (bg-game.js/domino-game.js) + مسار الذكاء في chess.js/dama.js + مفاتيح المستوى في القواميس الأربعة + جداول payouts. لم أمس منطق الغرف أو التخطيط أو الشارات — لا تداخل مع bg-rooms/do-rooms/timer-layout/bgdo-polish.

## v2.23 (2026-09-13 مساءً) — sam — رأس d0f6637، نشر 0df7686e

**جذر ثاني لخلل الحارس (بلاغ المالك: «لا تنشر ما قام به cat»)**: سطر الجلب كان `git fetch origin "$BRANCH"` — يجلب arena فقط؛ مرجع origin/samsung-fixes-20260911 يبقى قديماً للأبد فلا يرى الحارس دفعات cat أبداً (إصلاح الجولة السابقة أضاف منطق merge-base لكن على مرجع لا يُحدَّث!). **الإصلاح**: `git fetch origin --prune` يجلب كل الفروع ثم merge-base يقرر.
**دمج v2.21-cat (DTSG)**: تعارضا index (كاش main.js) وGITHUB_SYNC حُلا.
**كسر كاش حاسم (dtsg2)**: إعادة تسمية cat لمست نفس ملفات تعديلاتنا العشرية (state/utils/rami/engines/09-chrome/15 CSS/translations) — الأرقام القديمة كانت مطابقة لمنشور سابق فكان الزائر العائد سيأخذ كاشاً قديماً بلا DTSG. جرفنا 40 ملفاً متغيراً إلى dtsg2 في index + الصفحات الثماني (جرد 68 مرجعاً صفر مفقود).
**قيَم**: هوية DTSG حية (title/meta/sidebar/--gold #d9b45c/لوغو dtsg) + كل مكتسباتنا فوقها: عشرية 999.75 وfmt 77,70 وGB 3.33 واستمرارية رامي وواتساب العائمة (52px) + انحدار 13/13+33/33+100/100+46/46+18/18+30/30. md5 حي مطابق.
**لـ cat**: انتهى — خادم النفق يقدم الهوية الجديدة (static per-request كما ذكرت) والنشر Pages تم. ملاحظة: كسر الكاش صار dtsg2 (فوق dtsg1) — أي دفعة قادمة تعدل ملفات مشتركة يجب أن ترفع رقم الكاش بنفسها أو الحارس لا يكسر الكاش تلقائياً.

## v2.25 (2026-09-14 فجراً) — sam — رأس 62671fb، نشر 112c6bfb

1. **استئناف رامي تلقائي (الجذر الثاني لتجميد/خلفية بنية)**: أعدت سيناريو المالك حرفياً (جولة ضد بوتين → تحديث → فتح اللعبة من البطاقة) فكشف: الجولة محفوظة (saved:true) لكن الفتح يعرض شاشة الإعدادات فقط — لا شيء يستأنف! الاستئناف كان يعمل فقط من زر «ابدأ». **الإصلاح**: initRami يستأنف فوراً عند فتح اللعبة (بناء + لافتة + البوت يلتقط دوره). مثبت حياً: تحديث → فتح → PLAYING بنفس الشوط بلا أخطاء. (الغرف مستثناة — إعادة بناء الخادم.)
2. **أقسام/فلاتر فارغة تختفي**: عنوان «Crash» بالرئيسية يختفي مع شبكته عند تعطيل كل ألعابه (sheCrash/sheFeatured/sheInstant) + فلاتر التصنيف: يظهر فقط ما فيه ألعاب مفعلة (updateFilterChips) — مثبت حياً (كراش: none، فلتر crash مخفي).
3. **بطاقتا الطاولة/الضومنة بصور خاصة**: مشهد اللعبة + الأيقونة بإطار ذهبي وبيضاوي ناعم (512px، 8-9KB) — محمّلتان حياً 97px. (+ إزالة loading=lazy التي كانت تسقط صور بطاقات عند إعادة البناء.)
4. **about**: كتلة الهيرو («44 لعبة فريدة...» + 12,458 لاعباً/جاهزية) أزيلت كاملة بCSS ومفاتيحها الست — حياً: صفر أثر.
5. **الفوتر**: «— ألعاب الدكاء التقليدية الرقمية» أزيل من كل الصفحات (index + legal-ui للتسع) — صار «DTSG» فقط. مثبت حياً.
6. **أهداف غرفة الرامي** (طلب مسبق بنفس الجولة): تطابق نافذة اللعبة حرفياً لكل وضع (سامبل 201-801 / طالاج 301-1001 + شوط واحد) مع إعادة بناء القائمة عند تغيير الوضع وحفظ الاختيار الصالح.
7. **قيَم**: انحدار كامل أخضر (100/13/33/46/18) + تحقق جولة 7/7 + حي 5/5 (خطأ JS واحد ظهر أول مرة = 401 جلسة اختبار عابر، إعادة الفحص النظيف صفر) + md5 مطابق (5 ملفات). كاش: dtsg3 + lu4.
8. **ملاحظة معمارية للمالك**: «rn» هي فلات دوغ (رهان فوري) وليست الروندا الكلاسيكية «rd» — الألعاب الفورية لا تحتاج استمرارية (جولتها ثوانٍ) والغرف تعتمد سجل الخادم أصلاً؛ الاستمرارية الشاملة مطبقة حيث لها معنى (الرامي المحلي كاملاً).


## v2.26 (2026-09-15) — cat — غرف الطاولة والضومنة للإنتاج — رأس 4879072 + دمج 490abc8

> النطاق: تفعيل الغرف الجماعية (bg/do) مضيف+ضيف وضد البوت مع التسوية المالية + حماية رصيد الغرف + دمج كامل لعمل sam v2.23–v2.25 في نفس الفرع.

### الغرف — ملفان جديدان + تكامل المنصة:
1. **`backgammon-game/js/ui/bg-room.js` (جديد)**: وضع الغرفة للطاولة — المضيف سائق الحقيقة (state من seed)، الضيف يبني من بث init المكتمل؛ roll/move/endTurn عبر rmove؛ البوت (خبير) يلعب مقعد الضيف فارغًا بما فيه التمرير الإجباري عند no-moves (إصلاح تعليق الافتتاح)؛ إعادة المباراة تلقائية بعد matchEnd.
2. **`dominoes-game/js/ui/domino-room.js` (جديد)**: نفس العقد للضومنة — init/seed + play/pass/draw/nextround/rematch؛ إصلاح بذرة إعادة المباراة (كانت 1 دائمًا → عشوائية).
3. **`js/core/rooms.js`**: bg/do ثنائيتان في roomGameIds + خيارات الغرفة (طول مباراة bg 1/3/5 · هدف do 50/100/150/200 + سحب كلاسيكي/Block) + `_applyGameOpts` قبل البدء + **`_refreshGold`**: مزامنة رصيد الواجهة من الخادم عند بدء الجولة (الكل).
4. **`server.js`**: bg/do في ROOM_GAMES_ALLOWED + **حارس `/api/sync`**: داخل غرفة جارية، gold الواجهة يُرفض (الخادم مصدر الحقيقة) — كان خصم الرهان يُطمس عند إغلاق الصفحة (sync قديم يكتب فوقه). مدعوم بمراقب DB: صفر انعكاس بعد الإصلاح. + تتبع settleRound خلف DM_TEST_MODE فقط.

### إصلاحات جودة الغرف:
- domino-app: تحرير `busy` عند انتهاء الجولة بعد آخر play (كان يظل عالقًا فتعطل الأتمتة/الطبقة) — المستخدم اليدوي لم يكن متأثرًا (زر الطبقة يعمل) لكن البوت/المسبار يتوقفان.
- bg-room: no-moves pass للبوت + إعادة جدولة botTurn بدل الاعتماد على تدفق المضيف.
- settle-steadying في المسبار: قراءة رصيد الخادم بعد استقرار بث التسوية (مهلة قصيرة).

### دمج sam v2.23–v2.25 (نفس الفرع — تعارضات حُلت كلها):
- **server.js**: r2 العشرية (منزلتان) في settleRound غرفنا أيضًا + كل مسارات الأموال (سام).
- **index.html**: كاش موحد — dtsg3 (CSS فوق dtsg2 سام) + dtsg4 (JS الأساسية فوق unified13/tl2 عندنا و dtsg2/3 عند سام) + bgdo3 (ملفات المشروعين + bg-room/domino-room الجديدان). بطاقات سام الغنية + GAME_IMG bg/do سليمة.
- **الصفحات القانونية الثمان + 2fa/about**: نسخة سام (dtsg2 + lu4) — ترجماتنا dtsg2 مطابقة فلا خسارة.
- **رامي**: auto-resume عند الفتح + أهداف الغرفة الديناميكية + استمرارية (سام) — بلا مساس بغرفنا.
- **GITHUB_SYNC**: أقسام v2.22 (نحن) + v2.23/v2.25 (سام) متسلسلة بلا تكرار.

### الاختبارات (بعد الدمج — كلها خضراء):
- **مسبار الغرف (جديد) 32/32**: bg مضيف+ضيف + bg ضد بوت (خصم/matchEnd/تراكب/تسوية/رصيد) + do مضيف+ضيف + do ضد بوت.
- مراقب DB: خصم صحيح 100000→99990 لكل مستخدمي الاختبار وصفر انعكاس (15 دقيقة).
- AI expert **26/26** · BGDO تكامل 30/30 · لعب 18/18 · timer-seat 25/25 · قرعة 13/13 · BJMP 33/33 · رامي v19 100/100 · آس 21/21 · خبير ✓ · dama fixes 15/15 · node --check ✓.

### لـ sam (النشر):
1. نشر Pages (السكربت يحمل تلقائيًا) — كاش dtsg3/dtsg4/bgdo3 سيكسر الكاش للزوار العائدين.
2. إعادة تشغيل الخادم — **لا تغيير schema** (server.js فقط: حارس sync + bg/do مسموحان + r2 دمج عشرية سام نفسه).
3. رهانات غرف bg/do: الخصم عند البدء (rooms/start) والتسوية (settleRound w0/w1/draw) — رسوم 5% موحدة.

## v2.27 (2026-09-15) — الإصدار المدموج الكامل — رأس الفرع arena/01a0a670-digital-moroccan-casino

**أول فرع يجمع الخطّين كاملاً**: خط sam (02368ae: إصلاح _connect BASE-Guard + SSE withCredentials + شطرنج v19-Master + محرك لحاق رامي + BotsLedger) **+** خط cat (v2.26: غرف bg/do + حارس /api/sync + شارات المؤقت + خبير الطاولة) **+** 6 كوميتات v2.27 (انظر V227_PLAN.md).

### ما الجديد
1. **ضاما — قاعدة النفخ مثبّتة بقرار المالك**: الأكل إلزامي قانوناً + حرية تحريك أي قطعة؛ المُلزَمة التي تترك الأكل تُنفخ. رسالة النفخ (كانت في i18n بلا استخدام) صارت تظهر في المسارات الثلاثة، ورقاقة الرهان #damaStake أُتمت. الاختبارات صُححت لترسيخ القاعدة (fuzz 459/459 · متصفح 52/52).
2. **أمان (المستودع عام!)**: كلمات البذر من DM_SEED_SUPER_PW/ADMIN_PW/USER_PW · كلمة اختبار الإنتاج ⇒ DM_LIVE_PW · **dtsg.pages.dev أُضيف لقائمة CORS في cf-worker/worker.js** (كان ناقصاً منذ v2.24). ⚠️ **على المالك تدوير كلمات super/admin/player على الإنتاج فوراً.**
3. **blindResult**: فجوة pn/rps أُغلقت — الخادم يجمع الاختيارين سرياً ويبث الزوج معاً (tests/_blind_pair_test.js ‏9/9).
4. **اختبارات**: pw123⇒pw123456 (5 اختبارات) · _rn_settle_test صار 18/18 · عتبة خبير الضومنة 0.625.

### خطوات النشر
1. `git fetch` ثم نشر الواجهة: `bash scripts/deploy-pages.sh` (رموز الكاش: dtsg5 / bgdo4 / lu5 — روندا وبلياردو بلا تغيير).
2. الخادم: إعادة تشغيل بـ node ≥ 22.12 — **يفضَّل ضبط DM_SEED_* بكلمات جديدة** (لا أثر على الحسابات القائمة؛ البذر لقاعدة فارغة فقط).
3. الووركر السحابي (إن كان مستعملاً): إعادة نشر cf-worker/worker.js (تحديث CORS).
4. تحقق سريع: `node tests/_blind_pair_test.js` + غرف bg/do + رسالة النفخ في ضاما.
