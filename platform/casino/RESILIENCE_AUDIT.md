# تقرير المرونة + التدقيق — رامي الجماعي

التاريخ: 2026-08-24 · الحالة: **منفَّذ ومُختبَر بالكامل**

## 1. المتطلبات المُحقَّقة

| المتطلب | الحالة | الدليل |
|---|---|---|
| خروج/انقطاع/إغلاق لاعب لا يُجمّد الجولة | ✅ | السائق يلعب آلياً عن المنقطع (`_resilience_test` Res-B) |
| الجولة تستمر وفق القواعد نفسها أياً كان اتصال لاعب | ✅ | اللعب الآلي = سحب قانوني + رمي ورقة لا منتمية |
| الغرفة/الجولة مكتفية ذاتياً (لا تتأثر بإغلاق المتصفح) | ✅ | الحالة على الخادم (moveHistory) + سائق يُعاد تعيينه |
| العودة في أي وقت مباشرةً أو بكود الغرفة | ✅ | إعادة بناء كاملة من السجل عبر `room:replay` (Res-A) |
| يعمل على الهاتف وسطح المكتب | ✅ | `_mobile_compat_test` 10/10 |

## 2. البنية المعمارية للمرونة

### 2.1 نموذج «السائق» (Driver) للمؤقّتات
- **السائق** = لاعب واحد مسؤول عن إدارة انتهاء المؤقّت لأي لاعب (افتراضياً المُنشئ).
- **اللاعب الحالي المتصل** يلعب آلياً عن نفسه فور انتهاء وقته (يحافظ على ترتيب يده المحلي، ويمنع حركة متأخرة).
- **السائق احتياطياً**: إن لم يتحرّك اللاعب الحالي خلال **مهلة سماح 4ث** (مثلاً انقطع)، يلعب السائق عنه. هذا يحلّ التجمّد الجوهري السابق: `if (curP.id !== myPlayerId) return` كان يُسكت الجميع عند انقطاع صاحب الدور.
- **بثّ الحركة الدقيقة**: السائق يبثّ `autoTimeout {playerId, drew, discardCardId}` — لا يعيد الآخرون تشغيل المنطق محلياً (يمنع تباين ترتيب اليد)، بل يطبّقون الحركة نفسها بحذافيرها → تطابق تام.
- **تجاوز الفشل (Failover)**: الخادم يتتبّع `room.online` عبر اتصالات SSE؛ عند انقطاع السائق يُعاد التعيين لأقرب لاعب متصل (`reassignDriver`)، وعند عودة لاعب والسائق الحالي غير متصل يُعيَّن له (`markOnline`).

### 2.2 إعادة البناء عند العودة (Reconnect Replay)
- الخادم يُسجّل كل حركة في `room.moveHistory` (سقف 2000، يُفرَّغ عند مباراة جديدة).
- عند اتصال SSE لعضو في غرفة «playing» → يُرسل `room:replay {history}`.
- العميل `_netApplyReplay`: يبني الجولة من `init` ثم يعيد تشغيل كل الحركات (draw/discard/open/finish/addToMeld/autoTimeout/nextRound) صمتاً، ثم يعرض الحالة النهائية مرة واحدة.
- **حارس**: لا يُعيد بناء لعبة قائمة فعلًا (تظل متزامنة عبر `room:move` الحيّ) — الإعادة تهمّ العائد بعد إغلاق فقط.
- **بوابة hasHistory**: المالك يستضيف لعبة جديدة فقط عند `!hasHistory` (بدء أولي/مباراة جديدة)؛ عند العودة ينتظر إعادة البناء فلا يولّد بذرة متعارضة.

### 2.3 تقدّم ديمقراطي بين الأشواط
- `ramiNextRound`: أي لاعب يبني الشوط التالي محلياً ويبثّ `{fromRound, dedup}`.
- الخادم يُلغّم التكرار (`dedupSeen`)؛ العميل يحرس بـ `fromRound` (يتجاوز إن سبقه غيره). يُلغى مؤقّت الـ60ث عند أي تقدّم لمنع الازدواج.
- `startRematch`: أي مشارك يبدأ التصويت (الخادم ينشئ مرة واحدة)؛ `tryResolveRematch` يتحمّل غياب المُنشئ (يكتفي بموافقة ≥2 إن غاب).

## 3. تدقيق القواعد والمنطق

| النقطة | الحكم |
|---|---|
| اللعب الآلي عند انتهاء الوقت | ✅ قانوني: سحب من الدكة (إن لزم) + رمي ورقة. الرمي دائماً قانوني (لو كانت الورقة «منتمية» يبقى رميها صحيحاً، اختيار غير المنتمية مجرد تحسين) |
| اختيار الورقة المرمية | ✅ حتمي: أول ورقة لا تمدّ أي مجموعة على الطاولة، وإلا آخر ورقة — لا `Math.random` في الاختيار |
| تجميد البطاقات على الطاولة | ✅ لا يُمسّها اللعب الآلي (read-only عبر `doesCardFitAnyTableMeld`) |
| الدور الأول (يد كاملة، بلا سحب) | ✅ `turnPhase===WAITING_DISCARD` → لا سحب، رمي مباشر |
| مرور الدور بعد الرمي | ✅ `executeMove` يمرّر الدور داخلياً؛ `_processTurn` يزامن |
| بقاء القواعد/الشروط كما هي أثناء الانقطاع | ✅ لا تغيير في `rules` ولا `target`/`bet`؛ المحرك واحد، الحركات معاد تشغيلها حرفياً |
| التزامن بين المتصلين | ✅ بثّ الحركة الدقيقة (لا إعادة منطق) + حارس fromRound + إلغام الخادم |

**ملاحظة مهمة**: «حفظ الجولة ≠ تجميدها» — الجولة تستمر وفق القواعد حتى نهايتها القانونية بغضّ النظر عن اتصال أي لاعب، وهذا ما يفعله النظام.

## 4. توافق الهاتف / سطح المكتب

`_mobile_compat_test` (10/10) على إطارَي 390×844 (لمس) و1280×800:
- اللعبة تُحمَّل وتصل `PLAYING` على الإطارين.
- لا فيضان أفقي (العرض ≤ عرض الشاشة).
- عناصر اللوحة الأساسية موجودة.
- إعادة الاتصال تُعيد البناء على الإطارين.
- صفر أخطاء JS.

آلية المرونة كلها من جانب JS/SSE (بلا اعتماد على UI) فهي تعمل على المنصتين. أحداث اللمس/النقر للورق مستعملة من قبل (اختيار + سحب وإفلات).

## 5. تغطية الاختبارات

| الاختبار | النتيجة |
|---|---|
| `_resilience_test` (جديد: إعادة بناء + مؤقّت منقطع + تجاوز فشل السائق) | **10/10** |
| `_mobile_compat_test` (جديد: هاتف/سطح مكتب) | **10/10** |
| `_mp_e2e_test` (مزامنة لحظية بين متصفحين) | ✅ |
| `_req3_test` (تصويت المباراة الجديدة) | 10/10 |
| `_req678_test` (بنود 6/7/8) | 14/14 |
| `_spec_test` (المتفرجون) | 12/12 |
| `_score_test` (النقاط/العقوبات) | 17/17 |
| `_res_test` (الاستئناف) | 40/40 |
| `_iso_ui_test` + `_layout_test` + `_mpui_test` | ✅ |

**لا ارتداد (regression)** في أي اختبار سابق.

## 6. حالات حدّية وملاحظات

- **انقطاع الجميع**: تُوقف الجولة (لا متصل)؛ عند عودة أي لاعب يصبح السائق وتستأنف تلقائياً.
- **تذبذب SSE (blip) أثناء لعبة حيّة**: لا يُعاد البناء (اللعبة قائمة ومتزامنة عبر `room:move`).
- **سقف السجل 2000 حركة**: يكفي عدة مباريات كاملة؛ ويُفرَّغ عند كل مباراة جديدة.
- **عبة آلية عن لاعب متصل بطيء**: يمنحه مهلة 4ث ثم ينوب عنه — سلوك مقبول (انتهاء الوقت = يقرّر النظام).

## 7. اللاعب الآلي (AI) في الرامي — مُضاف

### 7.1 المتطلبات المُحقَّقة
| المتطلب | الحالة |
|---|---|
| اللعب ضد الآلي ممكن | ✅ لاعب فردي + غرفة جماعية + غرفة تدريب |
| فتح غرفة أو دخول جولة ضد آلي واحد أو أكثر | ✅ زر «أضف آلياً» في الغرفة + زر «🤖 الآلي» للتدريب السريع + اللعب الفردي |
| الآلي يلعب بدقّة 0% خطأ قانونياً بعدل | ✅ كل حركة عبر `getLegalMoves`+`executeMove` (المحرّك يرفض غير القانونية) |
| الحفاظ على قوانين وخصوصيات الرامي | ✅ لا تغيير في القواعد؛ الآلي يستعمل نظام الحركات القانوني نفسه |
| الآلي لا ينوب عن البشري استراتيجياً | ✅ عند انتهاء مؤقّت البشري تُجرى الحركة الصغرى (سحب+رمي) لتمرير الدور فقط |

### 7.2 البنية المعمارية
- **مقاعد الآلي الصريحة (`botSeats`)**: المحرّك يقبل قائمة بمقاعد الآلي (للغرف المختلطة بشر+آلي في مقاعد متفرّقة)، لا يفترض أن الآلي في المقاعد الأخيرة فقط.
- **في الوضع الجماعي السائق وحده يُشغّل البوت**: تجنّباً للتباين (اختيار الرمي عشوائي عند البوت). السائق يحسب حركة البوت ويبثّها (`draw`/`open`/`finish`/`discard`)؛ البقية يطبّقونها بحذافيرها. غير السائق لا يُشغّل البوت.
- **الخادم**: `/api/rooms/addBot` و`/api/rooms/removeBot` (المضيف فقط، أثناء الانتظار). اللاعب الآلي جاهز دائماً ولا يُعطى دور السائق أبداً (لا عميل له).
- **إعادة البناء تحفظ الآلي**: `room:replay` يتضمّن `botSeats` في تهيئة `init`، فتعود الجولة بالآليين أنفسهم عند الانقطاع.

### 7.3 القواعد والعدالة
- **0% حركة غير قانونية**: المحرّك (`executeMove`) يُحَقِّق كل حركة ويرفض المخالفة؛ البوت لا يلعب إلا ما هو قانوني.
- **اختيار الرمي**: عشوائي ضمن الحركات القانونية (عادل، لا تحيّز).
- **الافتتاح/الإنهاء/السحب**: يستعمل نفس قنوات اللاعب البشري مع التحقق الكامل.
- **توسيع المجموعات**: في اللعب الفردي فقط (محلي)؛ في الجماعي يُتخطّى لضمان تطابق تامّ بين الأطراف (ترتيب المجموعات قد يتباين عبر البثّ) — ولا يؤثّر ذلك على القانونية أو النقاط.

### 7.4 اختبارات الآلي (خضراء كلها)
- `_mpbot_test` (8/8): بوت في غرفة يلعب قانونياً + تزامن مضيف/ضيف.
- `_botflow_test` (5/5): لاعب فردي + غرفة تدريب + زر الإضافة.
- `_botresilience_test` (5/5): انقطاع المضيف في غرفة بالآليين → استعادة كاملة واستئناف.

## 8. البلياردو (4 أنماط) — مرونة أونلاين (2026-08-29)

### 8.1 المزامنة الحتمية (بث الوصف لا الحالة)
- الشبكة تنقل **وصف الضربة فقط** `{t:'shot', a(الزاوية), p(القوة), s(الدوران)}` + `place {x,y}` + `nom {n}` + `cfg {d,g}` — كل عميل يعيد تشغيلها على محركه.
- **حتمية مطلقة:** في وضع الغرفة يعمل `shootAndResolve` تزامنياً (بلا حلقة `requestAnimationFrame`) فيتطابق عدد خطوات الفيزياء حرفياً بين المُنشئ والضيف والمتفرج. (درس مُوثَّق: حلقة raf أرسلت ضربة بعدد خطوات مختلف → تباين بصمة الحالة.)
- **معيار القبول:** `_billiards_mp_test.js` **18/18** — بصمة حالة (`op/gr/balls` مُقرَّبة) **مطابقة بعد 20 ضربة** عند المُنشئ والضيف **والمتفرج**، وبثّ `cfg` للكاروم (ONE/5) مع بصمة متطابقة.

### 8.2 الانقطاع والعودة (replay)
- كل حمولة `rmove` تحمل `payload.action` ← الخادم يسجّلها في `room.moveHistory` (سقف 2000)؛ عند اتصال SSE لأي عضو في غرفة `playing` يُبثّ `room:replay`.
- `blApplyReplay`: يعيد بناء المحرك (`billiardsStart('room')`) ثم يعيد تشغيل السجل بالترتيب (`cfg` → إعادة تهيئة بالاختصاص/الهدف، `end` → إنهاء الإطار، والباقي `applyPayload` حتمي). إن وصل `room:replay` قبل فتح اللعبة يبقى في `_pendingReplay` ويستهلكه `blRegisterRooms` عند الفتح (نمط الشطرنج/الضامة).
- **مُختبَر:** اللاعب يُغلق الصفحة، تُلعَب ضربة أثناء غيابه، يعود بنفس الجلسة → أعاد بناء 21 ضربة وبصمته مطابقة للمُنشئ (بند 4 في اختبار MP).
- **ملاحظة:** العودة تُعرَف بالجلسة (cookie)؛ سياق متصفح جديد = زائر/متفرج بلا replay (سلوك الخادم الموحّد لكل الألعاب).

### 8.3 التسوية المالية والحوافز
- الرهان يُخصم عبر `takeBet` عند البدء (ويُصفَّر عند نقص الرصيد)؛ الفائز يستلم `2×` عبر `giveWin` في `blEndFrame` — **مُختبَر**: رهان 10 → −10/−10 عند البدء، استسلام → الخصم +20 (صافي +10).
- `end` (استسلام/فوز قانوني) تُبثّ فتُنهِي الإطار عند كل الأطراف، و«مباراة جديدة» = تصويت `startRematch` (تفريغ `moveHistory` يسمح بإعادة الاستضافة).

### 8.4 بوابات الأدوار
- المتفرج (المقعد الثالث في غرفة 2) **ممنوع من الضرب**: `blHumanTurn` يفحص `BILLIARDS.isSpectator` (ثغرة مُصلَّحة: كان مقعد 0 للمتفرج يمرّر ضربة شبحية).
- `PLACE` عن بُعد: موضع الكرة البيضاء يُبثّ `{x,y}` ويُتحقَّق منه بـ`validPlace` عند الطرفين؛ اختبار MP يمسح الشبكة بحثاً عن موضع صالح قبل كل ضربة فلا يتجمّد الإطار بعد خدش الافتتاح.
- «الضرب خارج الدور» مرفوض عند الطرفين (بند منع الضرب في اختبار MP).
