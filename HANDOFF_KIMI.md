# 🎰 Digital Moroccan Casino — ملف تسليم إلى Kimi Code CLI

> **إلى المساعد Kimi Code CLI:** هذا ملف تعريفي شامل بما أُنجز على منصة الألعاب.
> مهمتك: **دمج كل الإصلاحات والتحسينات والألعاب الجديدة المذكورة هنا في النسخة التي لديك، ثم تشغيل خادم المنصة على الهاتف داخل بيئة Ubuntu في Termux** (التعليمات في القسم الأخير).

---

## 1) نظرة عامة على المنصة

- **الاسم:** Digital Moroccan Casino — منصة ألعاب مغربية (41 لعبة) بواجهة عربية RTL ودعم 4 لغات: العربية، الدارجة المغربية (RTL بمحلية `ar-MA`)، الفرنسية، الإنجليزية.
- **البنية:** خادم Node.js واحد (`server.js`، منفذ 3000، بدون أي حزم خارجية — Node standard library فقط) + واجهة HTML/CSS/JS خالصة (بدون فريموركات).
- **الاقتصاد:** 10 كوينز = 1 درهم مغربي. نظام إحالة (بونص 10٪ من أول شحن). أدوار: سوبر أدمن / أدمن محدود / لاعب.
- **حسابات تجريبية:** `superadmin/super` · `admin/admin` · `player1/123`.

### هيكل المشروع
```
platform/casino/
├── server.js              # الخادم الكامل (API + static + جلسات + أدوار)
├── index.html             # التطبيق الرئيسي (SPA)
├── about/terms/privacy/fairness/provably-fair/2fa/contact/admins .html  # صفحات قانونية مستقلة
├── css/01..09-*.css       # 09-chrome.css = الهيدر العائم app-dock
├── js/
│   ├── core/  (state, api, utils, auth, rooms, fx, live, fair, session, audio)
│   ├── games/ (rami, dama, chess, parchisi, ronda, blackjack, crash,
│   │           billiards + billiards-physics + billiards-rules, group, catalog, engines)
│   ├── i18n/translations.js
│   ├── rules/game-rules.js
│   └── ui/legal-ui.js     # حقن الهيدر الموحد في الصفحات القانونية
└── tests/                 # أطقم اختبار Node (بدون تبعيات، تعمل بـ node مباشرة)
```

---

## 2) سجل الإصدارات المنجزة (يجب أن تكون كلها مدمجة عندك)

| إصدار | المحتوى |
|---|---|
| **v12–v13** | بلياردو: هيدر أيقونات وسجل رهان داخل صفحة اللعبة؛ ملء الشاشة v12c (حلقة افتراضية في `blFitCanvas` — **لا يُمس**)؛ انزلاق الكرة غرافيكياً نحو الفوهة |
| **v14–v15** | لعبة **غولڤازور** الجديدة (بلياردو مغربي بقواعد خاصة — انظر §3)؛ سنوكر: حفر وأعناق أصغر (pk=0.75، blNeckScale=0.9) |
| **v16/v16b** | نظام الإحالة (`GV<id36>-<6hex>`) + أدوار سوبر/أدمن كاملة API؛ أدمن محدود: تسجيل + شحن + إسكات ≥24h فقط؛ صفحة `admins.html` بشروط وعمولات الأدمنز (25–40٪ حسب الشرائح) بأربع لغات |
| **v17** | admins.html: حذف قسم السوبر أدمن وجدول المقارنة، إضافة قسم «التظلم والشكايات» و«صلاحيات الأدمنز»؛ حذف 7 صفحات قانونية مكررة من index.html (317 سطراً)؛ **هيدر app-dock موحد** في كل الصفحات القانونية عبر `js/ui/legal-ui.js` الجديد |
| **v18** | **إصلاح قيمة الآس السياقية في الرامي** + رفع بوتات الشطرنج والضامة لمستوى الخبير (انظر §4 و§5) |

---

## 3) الألعاب الجديدة والقواعد الحرجة (لا تُخالَف)

### غولڤازور (Golvazor) — بلياردو مغربي أصلي
- الطاولة والكرات = نسخة بلاكبول EPA (W1000×H500، R=12.8، baulkLineX=200) — **القواعد فقط مختلفة**.
- سقوط البيضاء = كرة بيد **خلف خط الباولك حصراً**.
- خطأ → جزاء ضربتين للخصم؛ **لا جزاء قبل تحديد المجموعات بدون أي استثناء**.
- جزاء الضربتين في «ديريكت» يبقى حتى مع السوداء الوحيدة.
- الكسر: سوداء وحدها = **فوز ساحق**؛ سوداء + كرة = **انتحار**؛ لونان ساقطان = اختيار يدوي بالنقر.
- أنونص: يتطلب لمس وسادة (بيضاء/سوداء) قبل سقوط السوداء وإلا انتحار `GV_SUICIDE_NORAIL`.
- 5 طرق إنهاء. الاختبار: `tests/_billiards_golvazor_test.js` (66 فحصاً).
- المواصفات الكاملة: `GOLVAZOR_SPEC.md`.

### قواعد بلياردو عامة
- لا هالة حول الكرات؛ خطوط التصويب حمراء عند التوجيه لكرة الخصم.
- **ملء الشاشة v12c ممتاز — ممنوع لمسه** (الحل: حلقة افتراضية في `blFitCanvas`، وليس letterbox).
- زر التنفيذ ‹ دائري ذهبي — استثناء صريح من الشفافية.

### الرامي (نمطان: سامبل/طلاج)
- سامبل: 104 ورقة، يد 13، عتبة افتتاح **51**، الجوكر = معكوس لون المؤشر.
- طلاج: 108 ورقة (4 جوكرات مطبوعة)، يد 14/15، عتبة **71**، الافتتاح يتطلب متتالية نقية + متماثلة نقية.
- الافتتاح يجب أن يتجاوز أعلى افتتاح سابق في الشوط.
- قانون الـ12 ورقة، جزاءات المرموق/لا تور +71.

---

## 4) 🔴 إصلاح v18 الحرج — قيمة الآس السياقية في الرامي (`js/games/rami.js`)

**الخلل:** البوت (والمدقق) كان يحسب **A = 10 دائماً** (`baseValue`)، فكان يفتتح بمجموعات لا تبلغ العتبة فعلياً، مثل:
`A-2-3 (يحسبها 15 والصحيح 6) + 9-10-J-Q (38) = "53" ≥ 51` بينما الصحيح **44 < 51** → افتتاح غير قانوني.

**القاعدة الصحيحة:**
- **A = 10** في المتماثلة (A-A-A) وفي **نهاية** المتتالية: `Q-K-A`، `J-Q-K-A`، `10-J-Q-K-A`.
- **A = 1** في **بداية** المتتالية: `A-2-3`، `A-2-3-4`، `A-2-3-4-5`...

**التنفيذ (ابحث عن وسم `[v18]` في rami.js):**
1. `MeldValidator._checkSequence(cards, jokerAllowed, aceMode)` — الفحص الداخلي أصبح يقبل وضع `'any'|'high'`، ودالة جديدة `isAceHighSequence()` تسأل: هل تصح المتتالية بالآس المرتفع؟
2. `RamiRules.cardPointsInMeld(card, meld)` — قيمة الورقة في سياق مجموعتها: آسُ متتاليةٍ لا تصح إلا بالآس المنخفض = **1**، وإلا = **10**. الجوكر = 0.
3. `RamiRules.meldPoints(meld)` — مجموع نقاط المجموعة بالمنطق السياقي.
4. `validateOpening()` — `freeScore/extraScore` يستعملان `cardPointsInMeld` بدل `baseValue`.
5. الخبير: `meldFreeScore` في `partitionSelectedCards` و`RamiExpertAI._freeScore` يستعملان `rules.meldPoints` — **تقييم البوت متطابق تماماً مع المدقق** فلا يفتتح أبداً بافتتاح مرفوض.

> ملاحظة: `baseValue` (A=10) تبقى مستعملة عمداً في **عدّ نقاط اليد المتبقية آخر الشوط** (J/Q/K/A = 10) — هذا صحيح قانوناً ولا يُغيَّر.

**الاختبار الجديد:** `tests/rami_ace_value_test.js` — **20/20** ✓ (يشمل حالات الانقلاب التي كان القديم يقبلها خطأً في السامبل والطلاج).

---

## 5) ✅ تدقيق v18 — البوتات على وضع الخبير 0% خطأ في كل ألعاب الذكاء

| لعبة | المحرك | الحالة بعد v18 |
|---|---|---|
| **رامي** | `RamiExpertAI` | خبير 0% (سحب/افتتاح/إنزال/رمي أمثل، حارس حصار الورقتين) + إصلاح الآس — تقييمه مطابق للمدقق |
| **شطرنج** | `chessPickMove` | **رُفع من عمق 2/650مث إلى عمق 5/2500مث** (`chessBotTurn`) — كان مستوى ضعيفاً غير لائق |
| **ضامة** | `DamaEngine.aiPick` | **بوت الغرفة يُثبَّت الآن على مستوى الخبير** (عمق 9/1600مث) في `damaStartRoom`؛ اللعب الفردي يحترم اختيار اللاعب (متوسط/محترف/خبير) وهو خيار مقصود مقابل مضاعِف الرهان ×2/×2.5/×3 |
| **بارشيسي** | `ParchisiEngine.aiPickOption` | خبير دائماً (`difficulty:'hard'`، `_pick` يختار الأعلى تقييماً بلا أي عشوائية خطأ؛ النرد وحده عشوائي عادل) |
| **بلياردو (كل الأنماط)** | `aiShot/aiPlan` | تصويب هندسي حتمي **بدون أي ضجيج عشوائي** — 0 استدعاء لـ random في التصويب |
| روندا/بلاك جاك/كراش | — | ألعاب حظ/توزيع — لا «خبرة» تُضبط؛ البوت يلتزم القوانين فقط |

**العدالة:** لا بوت يرى أوراقاً مخفية أو يغش — كل القرارات من المعلومات المشروعة فقط (اليد، الطاولة، المرمي المكشوف)، والرميات/التوزيعات من `RNG` عادل واحد للجميع.

---

## 6) الاختبارات (كلها خضراء — شغّلها بعد الدمج للتأكد)

```bash
cd platform/casino
node tests/rami_ace_value_test.js        # 20/20  [v18 جديد]
node tests/rami_expert_test.js           # سلامة 0 جزاءات + هيمنة الخبير
node tests/_billiards_golvazor_test.js   # 66/66
node tests/_billiards_physics_test.js    # 49/49
node tests/_billiards_eightball_test.js  # 62/62
node tests/_billiards_blackball_test.js  # 52/52
node tests/_billiards_snooker_test.js    # 39/39
node tests/_billiards_carom_test.js      # 24/24
node tests/_billiards_i18n_check.js      # 26/26
```
> الاختبارات التي تتطلب playwright (`_dama_fixes_test.js` وأمثالها) اختيارية — تخطّها إن لم تتوفر البيئة.

---

## 7) قواعد ثابتة يجب احترامها عند أي تعديل مستقبلي

1. **لا تمس** ملء شاشة البلياردو (v12c) ولا نهج الحلقة الافتراضية في `blFitCanvas`.
2. الدارجة المغربية **عربية RTL** (`ar-MA`) في كل مكان.
3. `admins.html` هي **المصدر الوحيد** لعمولات الأدمنز — باقي الصفحات تشير إليها بروابط فقط.
4. رفع كاش الأصول عبر `?v=YYMMDDxxx` عند أي تعديل JS/CSS (حالياً `260901v18`).
5. الصفحات القانونية تستعمل الهيدر الموحد المحقون من `js/ui/legal-ui.js` — أي صفحة قانونية جديدة تُنسخ من `admins.html`.
6. الخادم بلا تبعيات npm — أبقه كذلك.

---

## 8) 📱 تشغيل الخادم على الهاتف — Ubuntu داخل Termux

> نفّذ هذه الخطوات داخل بيئة Ubuntu (proot-distro) في Termux حيث تتواجد.

### أ) تجهيز البيئة (مرة واحدة)
```bash
# داخل Termux (إن لم تكن Ubuntu مثبتة):
pkg update -y && pkg install -y proot-distro
proot-distro install ubuntu
proot-distro login ubuntu

# داخل Ubuntu:
apt update && apt install -y nodejs npm git curl
node -v    # يلزم Node 18+ (يفضل 20)
# إن كانت النسخة قديمة:
# curl -fsSL https://deb.nodesource.com/setup_20.x | bash - && apt install -y nodejs
```

### ب) جلب المشروع ودمج التحديثات
```bash
# ضع نسخة المشروع في ~/casino (انسخها/افكّ الأرشيف/اسحب من الريبو حسب المتاح لديك)
cd ~/casino/platform/casino

# تحقق من وجود مكونات v17/v18 الأساسية:
grep -c "cardPointsInMeld" js/games/rami.js      # يجب > 0  (إصلاح الآس v18)
grep -c "chessPickMove(CHESS.state, 5" js/games/chess.js  # يجب 1 (خبير الشطرنج)
ls js/ui/legal-ui.js                              # هيدر الصفحات القانونية v17
grep -c "260901v18" index.html                    # كاش v18

# شغّل الاختبارات (القسم 6) — يجب أن تكون كلها خضراء
```

### ج) تشغيل الخادم
```bash
cd ~/casino/platform/casino
node server.js          # يستمع على المنفذ 3000

# للتشغيل الدائم في الخلفية:
nohup node server.js > /tmp/casino.log 2>&1 &

# للوصول من متصفح الهاتف نفسه:
#   http://localhost:3000
# للوصول من أجهزة أخرى على نفس الشبكة: استخدم IP الهاتف
#   ip addr show wlan0 | grep inet   →   http://<IP>:3000
```

### د) ملاحظات Termux مهمة
- شغّل `termux-wake-lock` في Termux قبل الدخول لـ Ubuntu كي لا يقتل أندرويد العملية عند إطفاء الشاشة.
- بيانات المنصة (حسابات/أرصدة) تُحفظ في ملفات json بجانب `server.js` — انسخها مع المشروع إن أردت الاحتفاظ بالحالة.
- لا حاجة لأي `npm install` — الخادم يعتمد مكتبة Node القياسية فقط.

---

*آخر تحديث: 2026-09-01 — بعد دمج v18 (إصلاح آس الرامي + خبير الشطرنج/الضامة). بالتوفيق! 🤝*
