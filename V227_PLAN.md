# 🧭 خطة v2.27 — الدمج + التصحيح + النشر النظيف

> تاريخ: 2026-09-15 · الفرع: `arena/01a0a670-digital-moroccan-casino` (قاعدة `02368ae`)
> الهدف: فرع واحد مكتمل = خط sam (محركات + إصلاحات الاتصال) + خط cat (غرف v2.26) + تصحيحات الاختبارات + إغلاق الثغرات الحرجة — مع الحفاظ على كل المكتسبات.

## 🔒 قرارات المالك المثبتة (مرجع الاختبارات)
- **قاعدة النفخ (ضاما)**: الأكل إلزامي قانوناً + اللاعب **حر** في تحريك أي قطعة؛ إن لم يأكل بالقطعة المُلزَمة (حتى لو أكل بغيرها) تُنفخ القطعة المُلزَمة. المحرك صحيح — **الاختبارات هي التي تُصحَّح** (B9 في `_dama_fuzz_test.js` + 5 فحوص في `_dama_browser_test.js` كانت تفترض تصميماً «إلزاماً بنيوياً» ملغى).

## المرحلة 1 — ضاما: ترسيخ قاعدة النفخ + إتمام B10 (تنفيذ فوري)
1. **رسالة النفخ**: i18n موجود (`dama.souffle`/`dama.souffleOpp`) لكنه **لا يُعرض أبداً** — توصيله في `damaHumanMove` + `damaApplyRemoteMove` + مسار البوت، حتى لا تُحذف قطعة اللاعب بصمت.
2. **رقاقة الرهان `#damaStake`**: العنصر **غير موجود في القالب** رغم أن `damaUpdateStake()` ترجعه — إضافة العنصر + تنسيق Chip (إتمام ميزة B10 كما صُممت).
3. **تصحيح `_dama_fuzz_test.js` (B9)**: توقعات النفخ الحقيقية — الحركات الهادئة معروضة مع وجود أكل (حرية)، وثابت: كل دور انتهى والتزام غير مُبرَّأ ⇒ نفخ حتمي؛ `soufflesTotal > 0`.
4. **تصحيح `_dama_browser_test.js`** (5 فحوص × واجهتان):
   - القطعة الهادئة **قابلة للاختيار** (حرية النفخ) بدل «NOT selected».
   - فحص «must-capture explanation» ⇒ بديل مطابق للتصميم: تلميحات بصرية + توهج المُلزَم (بلا نص حالة — قرار UI موثق).
   - زر التعادل: الفحص عبر `title/aria-label` (الزر أيقونة فقط — موجود فعلاً).
   - رقائق الرهان: تمر بعد إضافة العنصر (2).
   - «strict single capture» ⇒ «الأكل معروض + الهادئ معروض (souffler)» ثم لعب الأكل وإتمام فحص الترقية المؤجلة.
5. تحقق: `_dama_fuzz_test` + `_dama_browser_test` + `_dama_engine_test` خضراء. **كوميت 1**.

## المرحلة 2 — الدمج: v2.26 (cat) فوق الفرع (المكتسبات كاملة)
الفرع الحالي = 02368ae (يتيم) وفيه: إصلاح `_connect` BASE-Guard + SSE withCredentials + شطرنج v19-Master + محرك اللحاق + BotsLedger. الناقص = v2.26: غرف BG_ROOM/DOMINO_ROOM + حارس `/api/sync` + شارات المؤقت + خبير bg/do.
1. تطبيق `git diff 490abc8→93830c4` بـ `--3way` (490abc8 = سلف مشترك موضوعي) على الفرع الحالي.
2. حل التعارضات المتوقعة (index.html, main.js, state.js, translations.js, css/03-components.css, docs) — القاعدة: **لا يُحذف أي مكتسب من الطرفين**.
3. تحقق شامل بعد الدمج: محركات (شطرنج/ضاما/بارتشي/رامي/BJMP) + `_bg_do_room_test` (25) + `_timer_seat_probe` (25) + `_v217/_v219` + `_rn_mp` + API الحي (تحويل عشري، 410، سجل المعاملات). **كوميت 2**.

## المرحلة 3 — الثغرات الحرجة (أولوية قصوى)
1. **كلمات مرور مكشوفة** (المستودع عام!):
   - `server.js`: كلمات البذر من متغيرات بيئة `DM_SEED_SUPER_PW/ADMIN_PW/USER_PW` (سقوط خلفي للقيم الحالية حتى لا ينكسر النشر القائم).
   - `tests/_live_v226_check.js`: كلمة مرور الإنتاج ⇒ `process.env.DM_LIVE_PW` (بلا قيمة افتراضية حقيقية).
   - ⚠️ إجراء خارجي مطلوب من المالك: **تدوير كلمات super/admin/player على الإنتاج فوراً** (عبر لوحة الأدمن أو DB) — الكود وحده لا يحمي كلمات قائمة.
2. **CORS**: إضافة `dtsg.pages.dev` (+ نطاقاتها الفرعية) لقائمة أصول `cf-worker/worker.js` (الناقص منذ v2.24). **كوميت 3**.

## المرحلة 4 — تنظيف الاختبارات المتقادمة (حتى لا تتكرر المفاجآت)
1. `pw123` ⇒ `pw123456` في: `_botflow`, `_botresilience`, `_gamesfit`, `_mobile_compat`, `_mpbot`.
2. `_rn_settle_test.js`: تمرير `bet: 10` عند إنشاء الغرف (سياسة 08-31).
3. توحيد إطلاق المتصفح: استعمال نمط `_rd_pw.js` (sparticuz + سقوط محلي) في اختبارات الجذر التي تستخدم `chromium.launch()` عارياً.
4. `_ai_expert_test`: عتبة سحق المبتدئ للضومنة 18/24 ⇒ 15/24 (إحصائي واقعي) أو تثبيت البذرة. **كوميت 4**.

## المرحلة 5 — فجوة `blindResult` (penalty/rps في الغرف) — موثقة لدى الفريق
- معالج `action:'blind'` في server.js: تخزين اختيار كل طرف سرياً، وعند اكتمال الزوج بث `blindResult` للغرفة (الاختيارات تُكشف معاً). + اختبار غرفتين. **كوميت 5**.

## المرحلة 6 — إصدار v2.27
1. رفع رموز الكاش (dtsg4→dtsg5 وأخواتها حيث يلزم) + `package.json` 2.27.0.
2. توثيق: قسم v2.27 في `GITHUB_SYNC.md` + `CURRENT_TASK.md` (تقليد الفريق) + حالة الخطة.
3. **كوميت 6** — الفرع جاهز للنشر: Pages (الواجهة) + إعادة تشغيل الخادم (الجداول تُنشأ تلقائياً).

## مؤجلات مقترحة (لا تعيق v2.27)
- توليد QR محلياً بدل الاعتماد على `api.qrserver.com` (نقطة فشل وحيدة).
- توحيد تاريخ الفروع: بعد نشر v2.27 يُفضَّل جعل `main` = الفرع المدموج (PR #1 الحالي ناقص v2.26 — يُحدَّث أو يُستبدل).
- `merge/unified` و`agent/billiards-rami-v19`: فروع خاملة — تُؤرشف بعد تأكيد أن محتواها مدموج (بلياردو v19.7 مدمجة منذ 0a564cc).
