# نسخة احتياطية — التحسينات الجديدة (2026-08-28)

## المصدر
- النسخة الجديدة: `workspace-01a047b9-0944-7dd4-88b9-4c68ab6b3777.zip` → مجلد `casino/`
- النسخة السابقة: `tarikchouika/digital-moroccan-casino` (فرع `main` + فرع الاسترداد `recovery/2026-08-14-02-16-05-30`)

## ما الذي تغيّر

### 1. `2fa.html` — إعادة هيكلة جذرية
- **السابق** (فرع الاسترداد): 224 سطراً، بيانات i18n بصيغة inline `'key': { ar, fr, en }`.
- **الجديد** (casino/2fa.html): 309 سطراً، بيانات i18n بصيغة JSON `"key": { "ar","da","fr","en" }`.
- **إضافة لغة جديدة**: `da` (دانماركي) في كل الحقول.
- **النقل**: من جذر المشروع → مجلد `casino/`.
- ملاحظة: `main` الحالي لا يحتوي `2fa.html` أصلاً (حُذف)، فالنسخة الجديدة تُعيد إحياءه بهيكلية محسّنة.

### 2. ملفات جديدة كلياً (غير موجودة في الـ repo)
- 42 ملف اختبار آلي (`_*.js`) تغطي: رامي جماعي (dama/rami)، شطرنج (chess)، بارتشيسي (parchisi)، multi-player (mp)، متانة/مرونة (resilience/bot)، توافق الهاتف، والمزيد.
- `RESILIENCE_AUDIT.md`: تقرير تدقيق المرونة (انقطاع اللاعبين، إعادة البناء عند العودة، تجاوز الفشل).
- 4 لقطات شاشة (desktop/mobile لـ dama و open).

### 3. `about.html`
- موجود في `main` بـ 444 سطراً (سليم). نسخة zip فارغة → **تم استثناؤها** لمنع الكتابة فوق النسخة السليمة.

## الفوارق الكمية
| العنصر | السابق | الجديد |
|---|---|---|
| 2fa.html | 224 سطر (inline) | 309 سطر (JSON + da) |
| ملفات الاختبار | 0 | 42 |
| تقرير المرونة | لا | نعم |
| لقطات الشاشة | لا | 4 |

## الربط بمركز البيانات (الخادم)
- الخادم (`server.cjs`) يخدم الملفات الثابتة من جذر المشروع على `:8011`.
- بنسخ `casino/` إلى جذر المنصة، يصبح متاحاً عبر `/casino/2fa.html` مدعوماً بنفس قاعدة البيانات (مركز البيانات) وواجهات `js/` الحالية.
